# 易境 M0 MVP 发布说明

> 2026-09-05 数据架构说明：本版本已切换为 PostgreSQL 账户模式。个人数据经服务端 API 按账户隔离，浏览器不创建业务数据库；历史浏览器数据按当前决策不迁移。下方早期“本地数据库/IndexedDB”条目仅保留为历史变更记录，不代表当前运行时实现。

> 2026-09-05 复习指标一致性补充：今日总结、统计页累计/长期指标、到期完成率、最近学习位置和薄弱点列表现在统一排除结构损坏的复习状态，避免 NaN/非法日期等数据污染学习指标或薄弱点入口；完整 `npm run verify:release` 已复验通过：单元 374/374、内容 34/34、数据可靠性 159/159、构建 114/114、冒烟 30/30、无障碍 4/4、响应式 2/2、性能 4/4，生产 Chromium/WebKit 各 117/117，移动生产模拟 8/9（平板专属菜单按设计跳过）。

> 2026-09-05 详情状态一致性补充：八卦与六十四卦详情学习状态现在复用复习状态结构校验，损坏的复习卡显示“数据待修复”；完整发布门禁已复验通过，基础单元测试 369/369。

> 2026-09-05 复习队列状态隔离补充：队列、首页到期判定和“立即再练”现在复用同一复习状态校验，数字字段损坏的卡片会在展示前隔离并提示导出处理，不会等到提交时才失败。

> 2026-09-05 复习调度器与队列状态边界补充：`scheduleNext`、复习队列、首页到期判定和“立即再练”现在拒绝越界阶梯、无效本地日期、非有限或负的复习计数、非法薄弱标记及空状态，避免损坏数据被算术传播后写回或展示后才失败；迁移层仍负责修复已知旧记录。最新 `npm run verify:release` 通过：单元 366/366、内容 34/34、数据可靠性 155/155、构建 114/114、冒烟 30/30、无障碍 4/4、响应式 2/2、性能 4/4，生产 Chromium/WebKit 各 117/117，移动生产模拟 8/9（平板专属菜单按设计跳过）。

> 2026-09-05 设置偏好并发可靠性补充：主题与字号控件现在通过最新值引用成对应用视觉偏好，连续快速切换不会用旧的兄弟控件值覆盖当前样式；仓库边界、lint、typecheck 和构建均通过。

> 2026-09-05 收藏并发可靠性补充：收藏切换下沉为 repository 内事务性读改写，UI 使用事务返回的最终状态，避免跨标签页旧收藏快照导致反向操作；Chromium/WebKit 收藏相关生产场景各 6/6 通过。

> 2026-09-05 AI 授权快照可靠性补充：授权开关、数据范围和预览用途的快速连续变更现在统一从最新状态引用生成原子批量写入，避免旧闭包覆盖后续选择；Chromium/WebKit AI 设置生产专项各 1/1 通过。

> 2026-09-05 笔记删除可靠性补充：自动保存、软删除与撤销现在共用串行写入队列；删除状态在队列内先更新，避免待执行的自动保存将已删除笔记复活；编辑控件和删除/撤销按钮在写入期间禁用。Chromium/WebKit 生产删除流程各 2/2 通过。

> 2026-09-05 内容勘误可靠性补充：勘误保存与状态切换增加写入锁，异步写入期间表单和操作按钮禁用并显示保存状态，避免重复点击产生重复记录；Chromium/WebKit 内容勘误生产专项各 4/4 通过。

> 2026-09-05 推演快照可靠性补充：实验室快照异步保存期间标题、备注和提交按钮进入保存中状态并禁用，避免重复点击产生重复快照；Chromium/WebKit 生产专项各 1/1 通过。

> 2026-09-05 罗盘修正可靠性补充：偏差修正异步保存期间输入和提交按钮进入保存中状态并禁用，避免一次操作重复写入修正历史；Chromium/WebKit 生产专项各 1/1 通过。

> 2026-09-05 罗盘记录可靠性补充：坐向记录异步保存期间按钮进入保存中状态并禁用，避免重复点击产生重复观察记录；Chromium/WebKit 生产专项各 1/1 通过。

> 2026-09-05 最新统一发布门禁：`npm run verify:release` 无失败、无重试通过；单元 363/363、内容 34/34、数据可靠性 153/153、构建 114/114、冒烟 30/30、无障碍 4/4、响应式 2/2、性能 4/4，生产 Chromium/WebKit 各 117/117，移动生产模拟 8/9（平板专属菜单按设计跳过）。

> 2026-09-05 快速自测进度语义补充：进度条与一基数题目计数一致，首题显示 20%、末题显示 100%，并新增 `aria-valuenow` 生产回归；Chromium/WebKit 定向场景各 1/1 通过。

> 2026-09-05 稳定性补充：日期 rollover watcher 将轮询间隔限制在 1 秒～24 小时；无效值回退到 60 秒安全默认值，避免异常配置造成高频轮询；当前全量单元测试为 363/363，lint 和 typecheck 通过。

> 2026-09-05 冒烟预算补充：生产冒烟现在把启动等待、30 个路由和健康探针纳入同一 `SMOKE_TIMEOUT_MS` 截止时间；单请求超时会主动中止并返回明确错误，避免服务异常时发布门禁无限等待。

> 2026-09-05 PWA 生命周期补充：Service Worker 运行时缓存写入通过 `FetchEvent.waitUntil` 绑定请求生命周期，仅缓存成功响应；生产 Chromium/WebKit 的离线分享与版本升级场景均通过。

> 2026-09-05 来源编辑器生命周期补充：来源保存、追加、移除和模板粘贴在页面卸载后不再回写 UI，数据库提交与跨标签通知保持执行；生产 Chromium/WebKit 来源场景共 10/10 通过。

> 2026-09-05 日期展示生命周期补充：顶部日期标签的定时器和日期轮换回调增加挂载保护，路由切换后不再更新已卸载页面。

> 2026-09-05 日期适配器清理补充：日期 rollover watcher 停止后会忽略迟到的可见性或 pageshow 回调，避免清理竞态触发过期刷新。

> 2026-09-05 离线状态生命周期补充：OfflineStatus 的网络状态回调增加挂载保护，路由卸载后不会再写入过期提示状态。

> 2026-09-05 发布门禁复验：基础、内容、数据、构建、冒烟、无障碍、响应式、性能及 Chromium 生产门禁通过；WebKit 快速自测场景首跑页面设置阶段超时，重试通过并标记 flaky；移动生产模拟 8/9，平板菜单用例按设计跳过。

> 2026-09-05 WebKit 定向复验：快速自测场景独立重复 3/3 通过；完整长序列仍需在稳定 CI 环境复验资源稳定性。

> 2026-09-05 笔记生命周期补充：软删除后的撤销提示不再留下不可清理的定时器，组件卸载和撤销分支均释放计时器；保存/删除/撤销异步完成路径增加挂载保护；Chromium/WebKit 定向回归各 1/1 通过。

> 2026-09-05 数据备份生命周期补充：导出、导入、撤销导入和清空数据在异步事务完成后仅更新仍挂载的页面，避免切换路由后产生无效状态更新。

> 2026-09-05 罗盘记录生命周期补充：坐向记录与手动偏差修正提交后的页面状态更新增加挂载保护；已完成 Chromium/WebKit 罗盘专项 6/6 回归。

> 2026-09-05 AI 设置生命周期补充：授权偏好异步保存完成后仅更新仍挂载的设置页，默认关闭、不联网和用户确认边界保持不变。

> 2026-09-05 复习与跨浏览器补充：复习作答写入/掌握度同步在页面卸载后仅保留数据库提交，不再回写已卸载界面；首页延迟实验室增加 1.5 秒空闲兜底，解决 WebKit 未派发初始 IntersectionObserver 时实验室长期停留占位的问题。复习核心场景 Chromium/WebKit 各 1/1，WebKit 生产全量 117/117 通过。

> 2026-09-05 学习闭环补充：即时练习、课程完成和收藏异步写入在卸载后不再更新页面状态，相关数据仍会提交并广播；定向 Chromium/WebKit E2E 共 22/22 通过。

> 2026-09-05 内容治理补充：复核清单、来源登记表和历法样例文件载入完成后仅更新仍挂载的内容治理页面，避免离开页面后写入过期摘要；相关 Chromium/WebKit 场景共 4/4 通过。

> 2026-09-05 容器验收补充：最新容器构建使用非 root UID 1001，健康检查为 healthy，容器内生产冒烟 30/30；仍需目标云环境的独立部署证据。

> 版本：0.1.0  
> 日期：2026-09-04  
> 定位：个人易学学习工具，不提供自动断卦或决策建议

> 当前缓存版本为 `yijing-static-v70`。六十四卦索引与详情已移除学习阶段展示，《上下经卦名次序歌》和三步记法位于工具中心独立页面；升级需确认 v69 旧缓存清理。

> 外链安全补充（2026-09-04）：来源编辑器与内容设置页打开新窗口的外链均带有 `rel="noreferrer noopener"`，相关仓库边界测试已通过。

> HTTP 安全响应头补充（2026-09-04）：生产路由统一返回 `nosniff`、`X-Frame-Options: DENY` 和严格 `Referrer-Policy`；冒烟检查已逐路由验证 30 个入口的实际响应头，未引入内容或 PWA 行为变化。

> 部署健康检查补充（2026-09-04）：新增 `/api/health` 存活探针，强制动态生成并禁止缓存，仅返回版本、Service Worker 缓存版本和 PostgreSQL 存储模式，不包含用户数据；部署平台可用 `GET /api/health` 做基础可用性检查。

> 容器部署补充（2026-09-04）：根目录提供 standalone 多阶段 `Dockerfile`，运行时仅保留必要的 Next.js 文件并使用非 root 用户，Docker `HEALTHCHECK` 会调用不缓存的 `/api/health`；本地 Docker 构建需单独执行，尚未替代目标云平台的 TLS、反向代理、持久化策略和真实断网验收。

> Standalone 启动补充（2026-09-04）：宿主机 `npm run start` 统一通过 `scripts/start-production.mjs` 启动，并在启动前同步 `public` 与 `.next/static`；支持 `--hostname`/`-H`、`--port`/`-p` 的分离式或等号式参数，避免直接使用 `next start` 时的 standalone 不兼容警告。

> Standalone 启动复验（2026-09-04）：修正后生产冒烟 30/30、Chromium 生产 E2E 117/117 通过，`sw.js` 等静态资源正常返回。

> 启动契约回归补充（2026-09-04）：新增 standalone 启动入口、静态资源同步、参数契约和容器日志清理边界测试，当前单元测试为 360/360。

> 容器构建复验（2026-09-04）：本机 Docker 镜像构建和容器启动已成功，容器上的 `SMOKE_BASE_URL` 冒烟通过 30 个路由、`/api/health` 与安全响应头；这仍不替代目标云平台的 TLS、反向代理和部署后离线验收。

> 容器健康状态补充（2026-09-04）：本机 Docker `HEALTHCHECK` 报告 `healthy`，运行用户 UID 1001，确认运行时未使用 root。

> 容器当前构建复验（2026-09-04）：最新工作区执行 `npm run test:container` 通过，standalone 镜像以非 root 用户启动并报告 `healthy`，30 个生产路由冒烟全部通过；云平台、TLS、反向代理和部署后离线仍待外部验收。

> 容器构建器兼容补充（2026-09-04）：`npm run test:container` 默认探测 `docker buildx` 并优先使用 BuildKit；当前环境缺少 buildx 时会自动回退 legacy builder，避免 Docker 工具链差异阻断本地验收，可用 `CONTAINER_BUILDKIT=0` 显式回退。

> 容器端口隔离补充（2026-09-04）：容器验收默认使用本机临时宿主端口并同步给生产冒烟；需要固定端口时设置 `CONTAINER_PORT`，从而避免并行运行时争用默认 3200。

> 容器中断清理补充（2026-09-04）：`npm run test:container` 捕获 `SIGINT`/`SIGTERM`，中断验收会先删除显式命名的临时容器再退出，避免留下后台服务。

> 容器超时诊断补充（2026-09-04）：容器验收会按实际 Docker 子命令报告超时预算；容器清理支持 `CONTAINER_CLEANUP_TIMEOUT_MS` 独立有界配置，避免 Docker daemon 异常时清理阶段无限等待。

> CI 容器门禁补充（2026-09-04）：质量工作流新增独立 container job，在 Ubuntu runner 中重复镜像构建、容器启动和 30 路由生产冒烟；此证据仍不等同于目标云环境的发布验收。

> 容器脚本补充（2026-09-04）：`npm run test:container` 会自动构建镜像、检查默认用户非 root、等待 `HEALTHCHECK`、运行生产冒烟并清理容器；设置 `CONTAINER_SKIP_BUILD=1` 可在本地复用已有镜像，避免重复下载依赖；考虑到镜像源下 `npm ci` 的实际耗时，镜像构建默认 15 分钟超时，可用 `CONTAINER_BUILD_TIMEOUT_MS` 调整；CI 容器 job 预算为 20 分钟，且直接复用 Docker 构建阶段的依赖，不在宿主机重复安装。

> 生产冒烟端口隔离补充（2026-09-04）：本地 `npm run test:smoke` 默认使用临时端口启动 standalone 服务；部署环境仍可通过 `SMOKE_BASE_URL` 或 `SMOKE_PORT` 指定目标地址。

> 首屏性能补充（2026-09-04）：首页卦象实验室在接近视口时才动态加载，同时保留 `#lab` 锚点；首页测试在滚动到锚点后验证动爻与变卦交互。

> 延迟分包失败体验补充（2026-09-04）：动态实验室模块加载失败时会显示错误状态、重试按钮和独立路由回退，不会让首页其余内容变成空白。

> 哈希入口稳定性补充（2026-09-04）：首页实验室监听初始 `#lab` 与 `hashchange`，修复 WebKit 哈希滚动时序造成的占位状态；完整发布门禁已重新通过。

> 八卦工具上下文补充（2026-09-04）：`/tools/trigrams` 现在直接渲染独立工具页面，返回入口回到 `/tools`；学习地图入口 `/trigrams` 仍返回 `/learn`。变更后的 `npm run verify:release` 全部通过，Chromium/WebKit 生产各 117/117。

> 最新全量门禁复验（2026-09-05）：当前工作区 `npm run verify:release` 退出成功：363/363 单测、34/34 内容、153/153 数据可靠性、114/114 构建、30/30 冒烟、UX 4/4 + 2/2 + 4/4、Chromium/WebKit 生产各 117/117，移动生产模拟 8/9（1 项按设计跳过）。Firefox 稳定版/CI、真实设备、目标云部署、来源授权和 64 卦/384 爻人工复核仍未完成。

> 最新发布门禁（2026-09-05）：即时练习完成后可直接回到今日页继续每日闭环；内容治理页新增历法样例与来源登记表的下载、严格回载和只读摘要校验，报告通过后逐项展示六类边界的样例 ID、已核验数量和规则集来源，内容复核清单也支持严格回载、只读摘要、核验计数和发布阻塞项，未知来源、跨边界样例错配或 accepted 规则未核验来源会被拒绝，报告形状错误会显示具体原因；历法输入新增跨年、跨月与 UTC+14/UTC-12 边界回归；单爻笔记目标解析统一拒绝越界爻位；旧复习记录缺失本地日期时按用户本地日历回退；OfflineStatus 移到数据库门禁外，避免启动阶段漏掉断网事件；统计周期控件补齐 `tab`/`tabpanel` 语义关联；AI 草稿审阅状态通过 `aria-live` 播报并在进入编辑时自动聚焦；五行关系工具补充施受方向说明并修复 360/768px 移动布局横向溢出；九宫详情、干支关系详情和罗盘测验反馈补齐动态状态播报；首页“工具”入口统一进入工具集合，五行和干支关系工具详情页返回路径统一回到工具集合；Service Worker v60。363/363 基础单测、内容 34/34、数据可靠性 153/153、UX 无障碍 4/4、响应式 2/2、性能 4/4、生产构建 114/114、冒烟 30/30 和 Chromium/WebKit 各 117/117 已通过；移动生产模拟 8/9（平板专属菜单按设计跳过）；本轮 WebKit 串行与 Chromium 并行生产全量均 117/117 无失败；Firefox、真实设备、部署和内容来源授权仍需外部验收。

> 来源并发收敛修正（2026-09-01）：来源编辑器现在允许“来源列表更丰富但时间戳略旧”的跨标签快照收敛，避免 WebKit 延迟可见性隐藏已提交引用；新增领域单测后基础门禁为 342/342，WebKit 生产并发来源场景重复 20/20 通过。随后统一门禁已确认 WebKit 生产 113/113；稳定 CI、Firefox、真实设备、部署和内容来源授权仍需外部验收。

> 统一发布门禁修正（2026-09-04）：修复后的 `npm run verify:release` 已完整通过：基础单测 349/349、内容 33/33、数据可靠性 146/146、构建 114/114、冒烟 30/30、无障碍（Chromium/WebKit）4/4、响应式 2/2、性能 4/4、Chromium/WebKit 生产各 114/114，移动生产模拟 8/9（1 项按设计跳过）。统计周期、干支关系、河图洛书和学习罗盘模式均已验证键盘首末项跳转，三个模式组的动态面板已补齐 `tabpanel` 关联；来源追加已增加有界读后写收敛、旧版缺省来源类型等价去重和入口归一化，五行五元素关系方向说明、九宫详情、干支关系详情和罗盘测验反馈的动态播报已在 Chromium/WebKit 定向回归通过，WebKit 并发场景重复 20/20，本轮全量无失败或重试；Service Worker 已升级到 v56；Firefox、真机、部署和内容来源授权仍需外部验收。

> 内容交接模板补充（2026-09-01）：历法样例校验器提供“下载空白模板”，可将待填写的规则集和样例包直接交给内容负责人；模板下载不修改应用内容。生产 Chromium 专项回归 1/1 通过。

> 来源登记交接补充（2026-09-01）：内容治理页提供“下载来源登记表”和严格回载校验，导出并重新核验当前五类来源的元数据、收录策略和复核状态，供内容负责人补齐版本、链接、版权和责任人；重复 ID、未知字段或空登记会被拒绝，文件不包含经典原文，且不会写回应用。

> Firefox 预检加固（2026-09-04）：Playwright Firefox 启动预检现在默认 15 秒超时并返回具体启动错误；本机 Nightly 仍无法在预算内启动，需用稳定版 Firefox 或 CI 完成 S6-001 验收。

> Firefox 预算边界修正（2026-09-04）：开发态与生产态入口会跟随 `FIREFOX_LAUNCH_TIMEOUT_MS` 调整预检父进程预算，并对非法测试预算回退默认值，避免自定义环境变量造成误报；不改变本机 Nightly 尚未通过的验收结论。

## 本版本包含

- 学习地图：阴阳、五行、八卦、六十四卦结构、爻位、爻变与关系卦等基础知识；
- 五行工具与八卦卡片：展示三爻图、文本化阴阳构成、基础象意、五行和后天方位；八卦详情同步显示对应复习卡学习状态，并可直接完成即时练习；
- 六十四卦索引与详情：文王卦序、中文上下卦与卦符、六爻结构、关系卦、学习状态和来源状态；
- 卦象实验室：组合上下卦、选择多个动爻、查看本卦/变卦和计算过程、保存推演快照；
- 主动回忆与间隔复习：客观题、四级回忆评价、到期队列、薄弱卡片和复习统计；
- 即时练习：知识点详情保留每节 3 道基础题；八卦、五行和六十四卦详情/工具也提供目标专属题目，作答与正式间隔复习分开记录但共用复习卡调度；
- 个人笔记：标签、来源定位、基础 Markdown 预览、自动保存、软删除和撤销；
- 内容分层：知识详情内容块带有 editorial 类型、来源编号和“通用基础”体系标签，并由构建校验保证与兼容 body 字段一致；
- 本地数据管理：偏好设置、备份提醒、导出范围预览后 JSON 导出、预览后合并导入、成功导入后一键撤销最近一次导入、确认词清空；导入前恢复快照不进入导出文件；偏好页包含主题、字号、复习上限，并明确提示当前内置内容仅有简体版，不做未经复核的自动繁简转换；
- Manifest（含 192/512 品牌图标）、Apple PWA metadata、支持平台安装入口（含 iOS 添加到主屏幕说明）、生产环境 Service Worker、注册失败降级提示和离线状态提示；
- 快速自测：5 道基础题给出学习起点建议，不改写正式掌握状态或复习队列。
- M4-001～M4-005 AI 辅学前置能力：默认关闭，支持按范围和用途生成本地发送预览，草稿输出强制来源/免责声明，提供三类风险评测和不覆盖原笔记的本地审阅界面；M4-006/M4-007 已加入撤销授权、远端删除确认契约、供应商无关调用边界、请求体大小/序列化护栏及离线/超时/服务不可用回退策略；当前不联网、不读取整库，也不保存 AI 输出。
- 内容勘误：可在知识点和六十四卦页面记录疑问、建议文本、来源定位，并保留本地处理状态。
- 内容复核交接包：设置页可导出不含经典原文的 JSON 元数据清单，包含内容 ID、来源、版本、状态和发布门禁，并保留来源作者、出版信息、定位、访问日期与版权说明供内容负责人核对。
- 子页面统一导航：可从任意学习、工具、复习和设置页面回到其他一级入口；保留 `/review/session` 与 `/tools/trigrams` 兼容路径。
- 基础错误体验：提供自定义 404、错误重试页，以及 `/settings`、`/tools` 一级入口回退。
- 本地数据库保护：启动时执行旧记录归一化；升级失败或阻塞超时会进入只读错误页，避免继续写入不完整数据。
- 掌握度一致性：复习作答后的知识点掌握度重算在作答历史与进度表的同一事务中完成，降低多标签页并发写入时的旧快照覆盖风险。
- 掌握度时间线：`lastStudiedAt` 与 `updatedAt` 使用单调时间戳合并，旧标签页迟到写入不会让学习时间倒退。
- 掌握度时间线已补 Chromium/WebKit 浏览器级回归，覆盖真实复习作答到 IndexedDB 写回；当前单元测试为 21 个文件、131 个测试通过。
- 当前单元测试为 21 个文件、139 个测试通过；新增收藏状态跨标签页同步的开发态与生产态 Chromium/WebKit 回归各 1/1 通过。
- 备份导入会针对其中出现的概念作答历史重建掌握度，避免缺失或过旧的派生进度与作答记录不一致。
- 课程页重复开始或完成操作不会覆盖更高的已有掌握度；状态标签根据合并后的实际分数显示。
- 课程、导入和复习入口均会对异常旧分数做安全归一化，并以单调时间戳保存；生产 Chromium/WebKit 相关回归各 2/2 通过。
- 复习与即时练习共用事务化持久化服务，双标签页同时作答时会按数据库内最新复习阶梯推进，避免丢失一次进度。
- 课程详情进度控件已支持跨标签页刷新和过期查询保护，其他标签页完成课程后当前页不会继续显示旧状态。
- 卦象详情收藏控件已支持跨标签页刷新和过期查询保护，其他标签页收藏或取消收藏后当前页不会继续显示旧状态；开发态与生产态 Chromium/WebKit 回归各 1/1 通过。
- `/notes` 个人知识库现同时汇总笔记与收藏，支持按上下文、具体内容和关键词查找收藏；笔记搜索会包含内容标题，收藏目标缺失时显示明确的内容不可用提示，不会静默丢失本地记录。
- 收藏聚合已覆盖知识点、八卦和六十四卦三类详情目标，并在 Chromium/WebKit 生产构建下分别通过映射与筛选回归。
- `/notes` 已加入响应式矩阵，Chromium/WebKit 在 360/768/1024/1440px 下均无横向溢出且收藏入口可见。
- 历史生产全量验收（2026-08-28，早期构建）：Chromium 与 WebKit 默认 2 worker 均 86/86 通过，包含收藏聚合、具体内容筛选、三类目标映射、六十四卦索引收藏同步、六十四卦详情学习状态、干支知识详情学习状态、薄弱点正确回链、逐爻回链、未知目标保护、跨标签同步和 `/notes` 响应式回归。
- 逐爻笔记聚合回链已修复：按实际目标 ID 显示所属卦名与爻位，并回到详情页对应爻位锚点；Chromium/WebKit 各 1/1 浏览器回归通过。
- 内容运行时守门：根布局在服务端/构建期执行静态内容契约校验；内容 ID、来源、卦爻映射、练习和依赖关系损坏时不会静默生成页面。
- 练习内容守门：构建期同时检查每个八卦、五行元素和六十四卦至少拥有一条即时练习；六十四卦组合题统一提供三个有效卦名选项，所有非二选一题的选项均去重并至少提供三个选项，防止重复干扰项或题目退化。
- 内容复核汇总回链会校验稳定目标 ID；合法单爻反馈可回到对应爻位锚点，未知目标显示集合页回退，不会误链到其他卦。
- 薄弱点列表保留最近错误的题目/答案快照，并按复习卡回链到知识详情；损坏的历史时间显示为“日期待校验”，不渲染无效日期。
- 笔记、收藏、内容勘误、推演快照、坐向记录、罗盘修正历史和最近学习位置的时间展示统一经过安全格式化；损坏或无法解析的持久化时间显示为“日期待校验”，不把 `Invalid Date` 暴露给用户。
- 数据库启动归一化会严格校验 ISO 时间戳及时间分量范围（包括拒绝 `24:00:00` 的日期滚动值）；损坏的创建/更新时间回退到迁移时刻，损坏的可选删除、最近学习、最近复习和上次备份时间会移除或重置，避免坏值继续参与排序、单调合并、统计或备份提醒。
- 备份导入会进一步校验推演快照的动爻不重复、本卦与上下卦对应、变卦与动爻计算一致；结构矛盾的快照会在事务写入前拒绝，不污染现有数据。

## 数据与内容边界

- 笔记、作答、进度、收藏、偏好和推演快照只保存在当前浏览器的 IndexedDB 中，不需要登录；
- 导出文件可能包含私人笔记，用户需要自行保管并定期备份；
- 内置内容按 `canonical`、`editorial`、`computed` 和 `personal` 分层；
- 来源登记表已覆盖经典、书籍、视频、网页和个人记录，并为每条来源记录收录策略、复核责任人和版权说明；已核验来源缺少必要书目信息时会被内容校验拒绝；
- 64 条卦辞和 384 条爻辞已经建立结构化记录，但经典底本尚未由内容负责人确认，页面会显示“内容待校对”，不展示未经核验的原文；
- 卦辞与爻辞记录已预留可校验的类型化内容块容器，当前因底本未确认保持为空，不会生成或展示临时原文；
- M2 阶段 A 已加入十天干、十二地支静态学习卡片与练习，以及分层的干支关系、河图洛书和九宫工具；方位、月份、时段和数字关联仅为记忆提示，尚未接入历法换算；
- 天干/地支静态数据现通过严格 schema 校验字段、序号、枚举、来源和未知字段，仍不承担历法换算或自动排盘；
- 干支关系静态数据现通过严格 schema 校验关系类型、配对、标题、来源、状态和未知字段，并保持五合/六合/六冲数量门禁；
- 河图、洛书和九宫静态数据现通过严格 schema 校验字段、数量、数字/宫位唯一性、洛书总和及卦关联；
- 八方方向与罗盘盘层注册表现通过严格 schema 校验字段、数量、ID/标签唯一性、中心角度边界与顺序、来源和启用状态；当前仍只启用已复核八方层，不代表二十四山或流派判断；
- M3 阶段 A 已加入 360° 八方手动学习罗盘，并提供自由探索、隐藏标签和方位测验模式；角度映射、边界和旋转仅用于学习，不包含二十四山或流派判断；范围决策记录见 `docs/adr/0001-学习型罗盘范围.md`；
- 罗盘盘层开关已加入：当前只启用已复核的通用八方层，二十四山以禁用态展示并说明等待条件；
- M3-B01 已加入用户主动触发的设备能力探测；M3-B02 已加入用户点击后才启动的方向事件读取，优先使用 WebKit 罗盘字段，其他平台仅提供标注为“精度未知”的绝对 alpha 估算；M3-B03 已加入最近样本的环形稳定性提示和磁干扰回退建议；M3-B04 已加入可见、可重置并保留原因和时间的手动修正历史，修正只作用于实时传感器读数；拒绝、异常和停止均回退到手动模式，传感器读数与稳定性状态不写入坐向记录；范围见 `docs/adr/0003-设备方向能力探测范围.md`、`docs/adr/0004-方向传感器读取边界.md`、`docs/adr/0005-传感器稳定性与精度提示.md` 与 `docs/adr/0006-传感器手动偏差修正.md`；
- M3-A07 已加入手工坐向记录，可保存角度、标题、环境备注并从本地历史恢复；记录携带八方盘层和规则版本快照，不代表传感器测量；
- M3-B05 已加入平台诊断信息和 iOS/Android 真机验收记录模板；自动化浏览器通过不等于真实设备矩阵通过，诊断信息不会上传或写入坐向记录；
- M2-B01 已形成历法与干支时间边界 ADR 草案，换年、换月、换日、子时和时区规则仍需权威样例复核；
- M2-B02/M2-B05 已加入历法输入审阅页，可自由输入并校验 IANA 时区和规则版本，并逐项展示换年、换月、换日、子时、二十四节气和真太阳时边界状态；规则确认前不输出农历、节气或干支结果；
- 现代书籍、视频和网页只记录必要的来源定位，不复制未经授权的大段内容。

## 验证结果

以下检查在 2026-08-28 完成：

- `npm run typecheck`：通过；
- `npm run lint`：通过；
- 复习数据补充：复习卡现在持久化 `algorithmVersion`，数据库升级至 v9，旧卡会安全补齐为当前算法版本；显式未知版本会保留并从复习队列隔离，其他兼容卡仍可继续学习，不会被当前调度器静默解释；数据导出预览会单独标注待兼容算法卡片数量。
- 无障碍补充：浅色/深色主题统一提供 `:focus-visible` 焦点样式，并支持 `prefers-reduced-motion: reduce` 关闭平滑滚动、动画和过渡；
- `npm test -- --run`：20 个测试文件、128 个测试通过（新增本地日历日期格式化与真实性边界、经典内容校对状态/原文一致性门禁、备份复习日期真实性校验、备份来源访问日期真实性校验、来源链接协议校验、用户时间戳协议校验、迁移阶段无效复习/来源日期与复习状态边界归一化、30 天跨月复习演练、导入合并摘要决策与导入表身份映射回归，另含路由文档标题、内容复核元数据交接包与备份提醒阈值/无效时间边界、备份跨表引用校验与孤立记录拦截、到期卡完成率和推演独立完成率统计、多卡 14 天复习模拟、备份协议校验、备份信封元数据校验、AI 偏好安全校验、v1～v8 迁移样例、偏好值归一化、来源登记策略、迁移提醒、八卦题型、干支模板、干支关系、河洛九宫、历法输入、历法边界清单、7 日留存、7 日后记忆率、14 日薄弱点改善率、视觉偏好、Markdown 安全预览、AI 授权、草稿输出、安全评测、审阅状态机、删除、供应商适配与服务降级策略、AI 请求体大小/序列化护栏、AI 配置数值归一化与上限、校验后 JSON 快照、请求体失败分类、内容块来源策略、相关知识链接、学习罗盘、盘层门禁、设备能力探测、方向事件适配、传感器稳定性、偏差修正、平台诊断、坐向记录和历法输入/规则集契约校验、规则版本一致性门禁、内容勘误状态历史、跨 UTC+14 时区的本地日期调度、时区规范化和 ISO 小数秒毫秒精度回归）；
- 最新复验（2026-08-28）：`npm test -- --run` 为 20 个测试文件、128 个测试通过；复习卡 v9 调度算法版本迁移、显式未知版本保留/隔离、备份导出保留未知版本、备份信封元数据校验、来源链接协议和用户时间戳校验、导出/导入预览的只读一致性快照、今日摘要/统计/薄弱点与复习队列一致性、偏好设置表单跨标签同步、界面数据入口、今日页最近学习位置上下文跳转、AI 草稿编辑后直接接受最新文本、时区规范化及夏令时重复/不存在时间保护、ISO 小数秒毫秒精度、规则版本一致性门禁和 Service Worker 旧缓存清理回归已覆盖。
- 当前复验补充（2026-08-28）：从 v1 形状数据库升级时已直接断言 v11 `recoverySnapshots` 表创建；生产 Chromium/WebKit 迁移回归各 1/1 通过。
- 当前改动回归：开发态 Chromium/WebKit 全量 136/136 可执行用例通过、6 条生产专用用例按预期跳过；生产 Chromium/WebKit 默认 2 worker 全量各 71/71 通过；当前生产构建下未知算法隔离、备份导出、导入确认摘要竞态和 AI 草稿审阅场景在 Chromium/WebKit 各 1/1 通过；罗盘修正未保存草稿跨标签保护、初始空白笔记编辑器防覆盖、历法夏令时边界、显式 ISO 偏移输入和时区规范化性能基线场景在开发态 Chromium/WebKit 各 1/1 通过。
- `npm run build`：通过，生成 114 个静态页面；
- 根布局内容守门：随生产构建执行 `validateSeedContent()`，本次构建通过；
- 本轮追加内容关系回归：新增知识点前置/相关关系的自指、重复和未知目标校验，单元测试总数现为 129 个。
- 本轮罗盘回归：权限等待期间的重复点击、模式切换和方向事件监听取消已覆盖；生产 Chromium/WebKit 目标场景各 2/2 通过，当前生产全量验收为各 72 条用例。
- 本轮复验：复习持久化事务改造后，生产 Chromium/WebKit 全量验收各 76/76 通过；开发态全量运行 152 条用例，新增并发复习、课程掌握度和导入掌握度重建场景均通过。
- 本轮最新全量复验：生产 Chromium 与 WebKit 默认 2 worker 均为 72/72 通过；文档中此前的 71/71 条目保留为上一轮记录。
- 本轮移动生产矩阵：Pixel 5、iPhone 13 与 iPad Mini 触控配置共 6/6 通过，罗盘按钮状态和数据设置入口无回归。
- Chromium + WebKit Playwright：开发态全量 136 个可执行用例通过、6 个生产专用用例按预期跳过（每个浏览器各跳过 PWA 两项与 Service Worker 降级一项），生产单浏览器默认 2 worker 验收各 71/71 通过；新增今日页待复习摘要即时刷新、内容复核页逐项展示 64 条卦辞与 384 条爻辞映射、跨标签页笔记、偏好和来源编辑器同步、已打开详情编辑器同步、导入确认摘要按最新本地快照重算、初始空白笔记编辑器防止覆盖其他标签页新记录、传感器方向读取、深色主题语义表面、脏草稿保护、清空已有笔记后的空快照自动保存、空白编辑器与空来源编辑器不创建空记录、历法夏令时重复/不存在时间保护、显式 ISO 偏移输入和时区规范化性能基线等场景，并验证 BroadcastChannel 与 storage 回退在 Chromium/WebKit 均可用；移动触控用例已从桌面默认配置隔离，避免非触控上下文误执行；此前失败的两个 WebKit 用例是测试在 Dexie 初始化完成前直接操作 IndexedDB，现已增加页面就绪等待并复验通过；
- `npm run test:e2e:prod:chromium` / `npm run test:e2e:prod:webkit`：Chromium 和 WebKit 各覆盖 71 个生产用例；开发态快捷入口 `npm run test:e2e` 仍可用于本地迭代；Firefox 仍需在稳定版浏览器或干净 CI 复验；
- Chromium axe WCAG 2 AA 扫描：28 个核心、动态详情、复习深链和 404 页面无 violations，并检查路由文档标题可识别；
- `npm run test:perf`：Chromium/WebKit 索引搜索、爻点击和历法时区规范化审阅的浏览器内响应均低于 300ms；
- `npm run test:responsive`：Chromium/WebKit 对实验室、干支学习、干支关系、河图洛书、学习罗盘、历法输入、数据备份、AI 授权和内容复核页面覆盖 360/768/1024/1440px，无横向溢出；
- `npm run test:mobile`：Pixel 5、iPhone 13 和 iPad Mini 开发服务触控模拟共 6 个用例通过；`npm run test:mobile:prod` 在最新生产构建上同样 6 个用例通过，覆盖实验室爻变、罗盘输入、数据设置入口和触控布局无横向溢出；真机验收仍需单独记录；
- `npm run test:smoke`：生产启动后检查 30 个核心学习、工具、设置和内容详情路由、Manifest 的 standalone 配置、品牌图标 MIME 类型和 Service Worker 缓存版本；也支持通过 `SMOKE_BASE_URL` 检查已部署环境；
- `npm run test:e2e:prod`：使用已构建生产服务运行 Playwright E2E，Chromium/Firefox 默认 180 秒、WebKit 默认 300 秒总超时，并清理异常浏览器与本次启动的生产服务进程（保留测试前已存在的 3000 端口进程），可用 `PRODUCTION_E2E_TIMEOUT_MS` 调整；若需隔离浏览器，可运行 `npm run test:e2e:prod:chromium`、`npm run test:e2e:prod:webkit` 或 `npm run test:e2e:prod:firefox`；深色主题语义表面回归在 Chromium/WebKit 各 1 个用例通过；安装提示已改为挂载后读取浏览器状态，Chromium/WebKit 的 iOS 安装提示场景未发现水合错误；
- `npm run test:e2e:prod:chromium` / `npm run test:e2e:prod:webkit`：重新构建后默认 2 worker 运行 71 个生产用例全部通过，覆盖无障碍、核心流程、今日页待复习摘要即时刷新与最近学习位置上下文跳转、五行工具上下文恢复、统计指标、内容复核、内容复核清单下载、内容勘误汇总、八卦详情勘误、前置知识提示及本地数据变更刷新、跨标签页笔记与偏好同步、已打开详情编辑器同步、来源编辑器同步、导入确认摘要竞态、初始空白笔记编辑器防覆盖、传感器方向读取、深色主题语义表面、清空已有笔记后的空快照自动保存、空白编辑器与空来源编辑器不创建空记录、脏草稿保护、未知复习算法版本拒绝记分、历法夏令时边界、显式 ISO 偏移输入、时区规范化性能基线、偏好页繁简能力提示、核心阅读正文和笔记输入字号、Service Worker 注册失败降级、备份提醒、备份恢复、性能和响应式及带查询参数的离线分享链接；
- `.github/workflows/quality.yml`：干净 GitHub Actions 环境中的静态检查、生产构建、三浏览器生产 E2E、Pixel 5/iPhone 13/iPad Mini 触控模拟、冒烟和 standalone 容器构建/冒烟验收入口；浏览器矩阵不再用单一环境变量覆盖运行器，WebKit 使用 300 秒默认预算，Chromium/Firefox 使用 180 秒默认预算；首次运行结果需在仓库 CI 中确认；
- WebKit 备份恢复流程此前重复 5 次通过，本轮新增今日页最近学习位置上下文跳转、五行工具上下文恢复、空笔记快照、空编辑器边界、未知复习算法版本、导入确认摘要竞态、初始空白笔记编辑器防覆盖、历法夏令时边界、显式 ISO 偏移输入、时区规范化性能基线和 Service Worker 旧缓存清理生产用例通过，生产默认 2 worker 全量 71/71 通过；
- `public/sw.js`：Node 语法检查通过。
- 生产启动冒烟：核心页面、`/manifest.webmanifest`、`/sw.js`、`/icon.svg`、`/icon-192.png` 和 `/icon-512.png` 均返回预期状态与资源类型，并确认不存在的路由返回 404（跟随兼容入口重定向）。

## 已知限制与发布前复验

> 最新补充（2026-08-28）：生产 Chromium 与 WebKit 默认 2 worker 均已完成 94/94 全量验收，新增三类目标详情即时练习统一回写回归；数据库已升级至 v11，连续遗忘字段迁移、导入前恢复快照/撤销和旧备份兼容回归通过；本文件上文较早的 86/86、87/87、88/88、89/89、90/90、91/91、92/92、93/93 数字保留为历史复验记录。

> 最新补充（2026-08-29）：数据库访问边界已收敛到 `db/repository.ts` 与 `db/backup-repository.ts`，页面组件不再直接访问 Dexie 表；偏好、复习、课程进度和旧版勘误归一化也统一经 repository 入口调用，直接备份合并 API 也会在事务前固定归一化输入快照，新增边界与行为测试防止回归。复习调度器另增加运行时评价白名单，未知评价不会静默按 `hard` 分支推进；AI 草稿输出现在严格拒绝未知/重复输入范围、未授权请求范围和非字符串来源定位；笔记删除与撤销、勘误状态切换均改为原子更新，不再用旧快照覆盖其他标签页的最新内容；学习记录排序改为按实际 ISO 时间瞬间比较。`npm test -- --run` 当前为 23 个测试文件、156 个测试通过；重新构建后生产 Chromium 与 WebKit 默认 2 worker 均完成 98/98 全量验收，生产移动触控矩阵 Pixel 5、iPhone 13、iPad Mini 共 6/6 通过。
> 最新主动回忆补充（2026-08-29）：八卦详情即时练习新增后天方位自由填写题，并修复即时三爻排列题误用普通选项的问题；复习会话同步支持文本答案和统一答案归一化，逐爻排列、即时练习与复习会话输入提交在 Chromium/WebKit 生产场景各 1/1 通过。

> 最新无障碍补充（2026-08-29）：复习会话的答案反馈增加 `role="status"` 与 `aria-live="polite"`，文本、选择题和逐爻排列在提交后均能向辅助技术播报结果；`npm run test:a11y` 继续通过。

> 最新缓存补充（2026-08-29）：页面行为更新后 Service Worker 缓存版本已从 v13 升级到 v14，并在 Chromium/WebKit 的 PWA 缓存升级用例及生产冒烟中验证旧缓存清理和新版本命名。

> 最新缓存补充（2026-08-29，后续）：统计日期边界与时间排序行为调整后缓存版本已继续升级至 v18；Chromium/WebKit PWA 升级用例与生产冒烟均已按新版本通过。

> 最新备份引用补充（2026-08-29）：备份校验会对本版本已知复习卡交叉检查不可变练习定义，拒绝将卡片目标替换为其他合法对象；未来版本新增的未知卡 ID 仍保留不透明兼容策略。

> 最新生产复验补充（2026-08-29）：新增备份校验和损坏文件保护后，生产 Chromium 与 WebKit 默认 2 worker 均完成 99/99 全量验收；WebKit 备份提醒场景的偶发导航超时通过提高该测试的 DOM 就绪等待上限后复验通过。

> 最新作答数据补充（2026-08-29）：复习与即时练习现在会可选保存 `responseTimeMs`，写入、迁移和备份导入均限制为 0–24 小时整数毫秒；该字段仅用于未来本地学习分析，不影响判题、回忆评价或掌握度，也不会发送到远端。新增协议边界、迁移清理和生产 IndexedDB 写回回归，当前单元测试为 24 个文件、180 个测试通过。

> 最新导入一致性补充（2026-08-29）：导入冲突的内容相同比较改用固定码点排序，不依赖浏览器语言环境；新增对象字段插入顺序回归，当前单元测试累计 181 个通过。

> 最新撤销并发补充（2026-08-29）：撤销最近导入现在会在同一事务内读取最新恢复快照、校验并恢复，避免另一标签页完成新导入时旧快照覆盖新数据；新增 repository 事务边界回归，当前单元测试累计 182 个通过，Chromium/WebKit 导出→清空→导入→撤销场景各 1/1。

> 最新发布门禁补充（2026-08-29）：在包含启动归一化事务修复的最新构建上，WCAG 扫描 Chromium 1/1、响应式 Chromium/WebKit 2/2、性能 Chromium/WebKit 4/4、生产冒烟 30/30 路由通过；当前单元测试累计 184 个通过。

> 最新学习摘要一致性补充（2026-08-30）：今日摘要、今日任务、统计和薄弱点现在通过 `readStudySnapshot()` 在单一只读事务内读取复习卡、知识进度和作答记录，避免跨标签页并发写入时出现跨时间点指标；仓储边界回归已通过，当前单元测试累计 186 个通过。

> 最新学习数据失败体验补充（2026-08-30）：首页摘要、今日任务、统计和薄弱点读取本地数据失败时会显示明确的可访问错误提示，并提供“重新读取”操作，不再静默呈现初始空统计；深色主题已补齐危险语义表面。

> 最新学习数据加载体验补充（2026-08-30）：四个学习摘要入口首次读取本地数据时显示可访问的读取中状态，避免未完成查询期间呈现误导性的 0 条或空薄弱点；后续跨标签刷新保留已有数据，生产 Chromium/WebKit 相关场景各 9/9 通过。

> 最新基础门禁复验（2026-08-30）：错误体验改造后的 `npm run verify` 通过，单测 186/186、生产构建 114/114、冒烟 30/30；首页/统计/薄弱点相关生产 Chromium 与 WebKit 场景各 9/9 通过。

> 最新时间排序一致性补充（2026-08-30）：损坏时间戳之间的最终排序改用固定码点比较，避免浏览器语言环境差异造成列表顺序漂移；本地日期回归已通过，当前单元测试累计 187 个通过。

> 最新偏好设置加载边界补充（2026-08-30）：设置页首次读取本地偏好完成前显示读取状态并禁用控件，避免误提交未确认的默认值；读取失败会保留默认值并显示明确提示，仓储边界回归已通过，当前单元测试累计 188 个通过。

> 最新笔记聚合加载边界补充（2026-08-30）：`/notes` 首次读取笔记、收藏和来源索引时显示加载状态，读取失败时提供可重试错误页，不再把本地存储异常误显示为空知识库；笔记/收藏生产 Chromium 与 WebKit 相关场景各 17/17 通过，当前单元测试累计 189 个通过。

> 最新六十四卦索引加载边界补充（2026-08-30）：索引读取收藏/学习状态期间显示读取中状态，失败时提供可重试提示，不再把状态读取异常误显示为“未收藏/未学习”；索引、收藏和搜索相关生产 Chromium/WebKit 场景各 5/5 通过，当前单元测试累计 190 个通过。

> 最新详情学习状态边界补充（2026-08-30）：八卦与六十四卦详情共用的学习状态读取组件在首次读取期间显示状态，在读取失败时显示可重试提示，不再将存储异常静默回退为“未开始”；详情/状态同步生产 Chromium 与 WebKit 场景各 4/4 通过，当前单元测试累计 191 个通过。

> 最新学习地图进度边界补充（2026-08-30）：课程列表与首页学习卡片共用的进度状态组件在首次读取期间显示读取中状态，失败时提供“重试”操作，不再把本地存储异常静默回退为“未开始”；学习地图相关生产 Chromium/WebKit 场景 2/2 均通过，当前单元测试累计 192 个通过。

> 最新勘误汇总加载边界补充（2026-08-30）：首页本地勘误汇总首次读取期间显示读取状态，失败时提供可重试提示，不再把存储异常误显示为“当前没有记录”；内容复核与单爻回链生产 Chromium/WebKit 场景各 2/2 通过，当前单元测试累计 193 个通过。

> 最新收藏状态加载边界补充（2026-08-30）：详情页收藏按钮首次读取期间显示读取状态，失败时提供可重试提示，不再把存储异常静默显示为“未收藏”；收藏、跨标签同步与知识库聚合生产 Chromium/WebKit 相关场景各 6/6 通过，当前单元测试累计 194 个通过。

> 最新来源编辑器加载边界补充（2026-08-30）：来源字段首次读取期间显示读取状态并禁用写入控件，读取失败时保护当前表单内容并提供重试；已有脏草稿不会因重试成功而被覆盖，来源同步与空来源生产 Chromium/WebKit 场景各 2/2 通过，当前单元测试累计 195 个通过。

> 最新发布门禁复验（2026-08-30）：`npm run verify:release` 在当前构建完整通过，基础门禁单测 195/195、生产构建 114/114、冒烟 30/30、Chromium 99/99、WebKit 99/99、移动生产模拟 6/6；Firefox 仍需稳定版或 CI 单独验收。

> 最新深色错误态补充（2026-08-30）：来源、收藏、勘误和学习地图进度的读取错误状态均补齐深色主题语义表面与可读文字颜色，新增样式边界回归；`npm run verify` 当前单元测试累计 196 个通过。

> 最新详情勘误加载边界补充（2026-08-30）：内容详情的本地勘误记录首次读取期间显示读取状态，失败时保护勘误表单并提供重试，不再把读取异常误显示为空列表；勘误创建、状态切换、单爻回链和跨标签场景生产 Chromium/WebKit 各 3/3 通过，当前单元测试累计 197 个通过。

> 最新详情目标切换边界补充（2026-08-30）：内容详情复用勘误组件实例切换目标时会清空旧列表并重新进入读取态，避免短暂展示或编辑上一个内容的反馈；目标切换守护已纳入边界回归，基础质量门禁保持通过。

> 最新详情勘误深色错误态补充（2026-08-30）：详情勘误读取失败提示补齐深色主题语义背景和文字颜色，避免错误状态在深色主题下对比度下降；样式边界回归保持通过。

> 最新学习状态目标切换补充（2026-08-30）：课程进度与详情学习状态组件在目标切换时重置旧状态并重新读取，避免短暂显示上一个知识点的掌握度；相关生产 Chromium/WebKit 场景各 3/3 通过，当前单元测试累计 197 个通过。

> 最新收藏目标切换补充（2026-08-30）：收藏按钮复用实例切换内容目标时重置旧收藏状态并重新读取，避免在异步查询完成前误显示或操作上一条收藏；收藏、跨标签同步与知识库聚合生产 Chromium/WebKit 场景各 6/6 通过。

> 最新统计日期排序补充（2026-08-30）：七日留存、七日记忆率和薄弱点改善率统一使用固定码点日期比较，不依赖浏览器语言环境；新增本地日期排序回归，当前单元测试累计 199 个通过。
> 最新复习总结日期排序补充（2026-08-30）：复习完成页选择本组最早下次复习日期时统一使用本地日期比较器，避免默认字符串排序规则随运行环境变化；基础质量、无障碍、性能和响应式门禁均通过。
> 最新前置知识读取状态补充（2026-08-30）：前置知识提示在异步读取掌握度期间显示可访问的读取中状态，避免把暂时空列表误认为没有前置要求；读取失败仍保留可继续浏览的明确提示。
> 最新 AI 设置读取状态补充（2026-08-30）：AI 授权设置首次读取期间禁用授权、范围和预览操作，失败时显示可重试状态，并监听跨标签数据变更，避免默认关闭值覆盖尚未确认的本地设置；当前单元测试累计 200 个通过。
> 最新数据备份读取状态补充（2026-08-30）：数据安全页读取备份提醒和恢复快照期间显示状态，读取失败时保留明确错误并支持就地重试；恢复入口在元数据读取完成前保持禁用，避免误操作；当前单元测试累计 201 个通过。
> 最新快速自测读取状态补充（2026-08-30）：自测结果读取期间不再立即展示默认入口，答题开始后也不会被跨标签刷新覆盖；读取失败提供重试，完成保存后发送本地数据变更事件；当前单元测试累计 202 个通过。
> 最新罗盘修正历史读取状态补充（2026-08-30）：修正历史首次读取期间显示状态，读取失败时保留已有历史并提供就地重试，表单不会被异步刷新覆盖；当前单元测试累计 203 个通过。
> 最新罗盘修正错误主题补充（2026-08-30）：修正历史读取错误态加入深色主题语义表面与对比度保护，避免在深色模式下错误提示不可读；当前单元测试累计 203 个通过。
> 最新推演快照读取状态补充（2026-08-30）：实验室快照首次读取期间显示状态，读取失败时保留已有快照并提供就地重试，异步刷新不会覆盖当前实验内容；当前单元测试累计 204 个通过。
> 最新坐向记录读取状态补充（2026-08-30）：坐向历史首次读取期间显示状态，读取失败时保留已有记录并提供就地重试，异步刷新不会清空用户记录；当前单元测试累计 205 个通过。
> 最新复习队列读取状态补充（2026-08-30）：复习队列读取失败时提供就地重试，重试过程取消过期请求并保留算法版本隔离提示；当前单元测试累计 206 个通过。
> 最新课程进度读取错误补充（2026-08-30）：课程进度读取失败时禁用开始/完成里程碑写入，显示可访问错误并支持就地重试，避免在状态未知时误写学习进度；当前单元测试累计 207 个通过。
> 最新备份操作互斥补充（2026-08-30）：导入、导出和清空本地数据使用统一 busy 锁，按钮与处理函数双重互斥，避免预览读取、写入或删除操作交错；当前单元测试累计 208 个通过。

> 最新生产全量补充（2026-08-29）：启动归一化事务修复后的最新构建，生产 Chromium 与 WebKit 默认 2 worker 全量各 99/99 通过，包含备份恢复、撤销导入、跨标签并发、PWA、性能、响应式和无障碍场景。

> 最新即时练习补充（2026-08-29）：完成态“再练一次”现在显式重置计时和作答状态，避免单题场景因题目 ID 不变而沿用上一轮耗时；生产 Chromium/WebKit 即时练习回归各 2/2 通过。

> 最新全量回归补充（2026-08-29）：包含即时练习重练修复的当前生产构建，Chromium 与 WebKit 默认 2 worker 再次各 99/99 通过；无障碍、PWA、数据恢复、跨标签同步、性能和响应式场景均无回归。

> 最新移动回归补充（2026-08-29）：当前生产构建的 Pixel 5、iPhone 13 与 iPad Mini 触控配置共 6/6 通过，实验室爻变、罗盘输入和数据设置入口无回归；真实 iOS/Android 硬件与输入法遮挡仍需人工验收。

> 最新质量命令补充（2026-08-29）：新增跨平台 `npm run verify`，串行执行单元测试、lint、typecheck、生产构建和 30 路由生产冒烟；任一步失败即停止并返回非零退出码。

> 最新数据摘要补充（2026-08-29）：清空数据前的数量确认现在通过单一只读事务生成一致快照，减少多标签页并发写入造成的摘要偏差；清空本身仍在独立写事务中执行。

> 最新发布命令补充（2026-08-29）：新增 `npm run verify:release`，统一运行基础质量门禁、Chromium/WebKit 生产 E2E 和移动端生产模拟；Firefox 不在本机发布命令中假装通过，仍需稳定版或 CI 验收。

> 最新发布命令实跑（2026-08-29）：`npm run verify:release` 已完整通过：单测 184/184、生产构建 114/114、冒烟 30/30、Chromium 99/99、WebKit 99/99、移动生产模拟 6/6。

> 最新发布回归复验（2026-08-30）：学习摘要快照改造后再次运行 `npm run verify:release`，单测 185/185、生产构建 114/114、冒烟 30/30、Chromium 99/99、WebKit 99/99、移动生产模拟 6/6 全部通过；Firefox 仍需稳定版或 CI 单独验收。

> 最新 Firefox 复验补充（2026-08-29）：本机 Playwright Nightly 在启动阶段无测试输出，生产单场景与筛选场景均达到受控超时；该结果仅记录为环境阻塞，不代表 Firefox 通过或产品失败，仍需稳定版 Firefox/CI 环境复验。

> 最新测试补充（2026-08-29）：备份导入冲突改为按实际时间瞬间判断，并新增带时区偏移的回归；时间戳比较还拒绝 Date.parse 可误认的非 ISO 值，备份协议、备份提醒、AI 草稿、统计、快速自测结果、数据库迁移、AI 远端删除请求和内容复核交接包统一复用严格解析器，来源链接还必须包含真实 HTTP(S) 主机；备份信封、数据表记录及嵌套来源/历史拒绝未声明字段；已知复习卡的目标类型与目标 ID 必须匹配不可变练习定义，未知卡 ID 保持不透明兼容；掌握度遇到未知评价或非有限分数时安全降级，不误显示为已掌握；偏好写入拒绝未知键和非法值；数据库启动归一化补齐收藏与推演快照的派生字段修复；迁移提醒的 `detectedAt` 及其他未声明字段现在均拒绝；AI 草稿审阅状态机拒绝未知事件、损坏状态和非字符串编辑内容；AI 预览拒绝顶层未知字段和未知排除字段，AI 供应商适配层拒绝篡改的授权预览、非对象请求和非对象选中数据，并拒绝 JSON 序列化后变成空值、数组、丢失字段或新增未授权字段的快照；当前单元测试为 24 个文件、178 个测试通过。

> 最新浏览器复核（2026-08-29）：在同一生产构建上重新执行 `npm run test:a11y` 并通过；生产 Chromium 与 WebKit 默认 2 worker 全量各 98/98 通过，未发现由本轮 AI 运行时边界变更引入的页面回归。

> 最新安全链接补充（2026-08-29）：Markdown 预览现在只接受带真实主机的 `https` 链接和格式正确的 `mailto` 链接，`http`、伪 HTTPS 和无效邮箱均按纯文本处理；Service Worker 缓存版本已升级为 `yijing-static-v20`，生产 PWA 升级和冒烟检查使用新版本。

> 最新内容契约补充（2026-08-29）：来源登记以及知识点、内容块、练习、八卦、卦辞和爻辞 schema 现在均拒绝未声明字段；内容复核交接包在序列化边界进一步校验枚举、清单 ID 唯一性、统计、发布门禁一致性和来源引用完整性，并拒绝伪造可发布状态、不合法来源链接或未知/重复/空来源引用；相关回归测试已覆盖，避免编辑或导入时静默丢弃拼写错误。

> 最新五行数据补充（2026-08-29）：五行种子数据现在通过可独立调用的 `assertFiveElementDataset` 在构建期校验记录字段、颜色格式、ID/名称唯一性及完整的相生/相克环，并有错误关系、颜色和未知字段回归；该校验只保证学习工具数据一致，不生成个人吉凶判断。

> 最新罗盘内容补充（2026-08-29）：八方方向表与盘层注册表已增加独立严格 schema 门禁，覆盖未知字段、角度边界/顺序、中心角度与方向 ID 映射、来源、启用状态与待确认原因；生产 Chromium/WebKit 罗盘目标场景各 4/4 通过，仍不启用二十四山或流派判断。

> 最新 AI 评测内容补充（2026-08-29）：三类 AI 风险评测案例已纳入严格 schema 与构建门禁，校验风险覆盖、案例 ID、输出类型、必含/禁用标记和来源数量；该改动不代表真实模型或远端服务已接入。

> 最新 AI 输出安全补充（2026-08-29）：供应商草稿的顶层对象与来源引用现在拒绝未声明字段，相关异常回归已通过；真实模型和远端服务仍未接入。

> 最新历法输入安全补充（2026-08-29）：历法输入与规则集现在拒绝未声明字段，未知字段回归已通过；规则集仍处于待确认边界，不会生成自动排盘结果。

> 最新历法生产回归（2026-08-29）：规则边界收紧后，Chromium/WebKit 历法输入、夏令时边界、显式偏移与时区性能目标场景各 4/4 通过。

> 最新体验门禁补充（2026-08-29）：最新构建下无障碍扫描 Chromium 1/1、响应式 Chromium/WebKit 2/2、性能 Chromium/WebKit 4/4 均通过；生产冒烟检查 30 个路由全部通过。真实设备、部署环境和长期使用仍需单独验收。

> 最新备份字段补充（2026-08-29）：备份信封、数据表记录及嵌套来源/勘误历史拒绝未声明字段；保留已声明的 `algorithmVersion` 和可选导出元数据兼容性。新增未知字段导入回归，当前单元测试为 24 个文件、177 个测试通过。
> 最新导入快照补充（2026-08-29）：导入归一化只保留备份协议定义的数据表，过滤 UI 预览元数据并复制嵌套集合；调用方后续修改原始解析对象不会改变待合并快照。

> 最新备份生产回归（2026-08-29）：严格 schema 改造后，生产 Chromium/WebKit 的备份入口、预览、合并、坏文件、孤立引用、提醒和导出→清空→导入恢复演练各 8/8 通过。

> 最新知识库快照补充（2026-08-30）：`/notes` 的笔记与收藏聚合改用单一 IndexedDB 只读事务读取，避免跨标签写入时出现跨时间点混合列表；repository 边界回归已覆盖，当前单元测试累计 209 个通过。

> 最新复习队列快照补充（2026-08-30）：复习会话在单一 IndexedDB 只读事务内读取复习卡状态与新卡/批次偏好，再构建到期队列；并发改动时不会混用不同时间点的设置和卡片状态，当前单元测试累计 210 个通过。

> 最新六十四卦索引快照补充（2026-08-30）：六十四卦索引在单一 IndexedDB 只读事务内读取收藏与学习状态，避免同一张卡片短暂展示跨时间点的两个状态；当前单元测试累计 211 个通过。

> 最新勘误汇总快照补充（2026-08-30）：首页勘误汇总在单一 IndexedDB 只读事务内读取待处理列表与已处理数量，避免跨标签更新时出现列表和计数不一致；当前单元测试累计 212 个通过。

> 最新复习总结会话 ID 补充（2026-08-30）：复习完成页的会话笔记使用每次会话唯一目标 ID，避免同一天多次复习覆盖此前的总结；当前单元测试累计 213 个通过。

> 最新备份元数据快照补充（2026-08-30）：数据设置页在单一 IndexedDB 只读事务内读取恢复快照和上次导出时间，避免并发导入/导出期间提示状态不一致；当前单元测试累计 214 个通过。

> 最新偏好快照与原子写入补充（2026-08-30）：设置页、全局主题水合和 AI 设置统一从单一 IndexedDB 偏好快照读取；AI 授权相关三个偏好使用一次事务写入，避免部分保存造成组合状态不一致；当前单元测试累计 215 个通过。

> 最新 AI 偏好写入顺序补充（2026-08-30）：AI 设置页增加串行写入队列，确保快速连续的授权与预览目的操作按顺序完成原子持久化；Chromium/WebKit 偏好与 AI 定向回归各 4/4 通过。稳定版 Firefox、真机和内容来源复核仍是发布前置条件。

> 最新普通偏好写入顺序补充（2026-08-30）：主题、字号和复习参数改用串行原子写入，快速连续调整不会因异步完成顺序导致旧值覆盖新值；完整质量门禁通过。稳定版 Firefox、真机和内容来源复核仍是发布前置条件。

> 最新串行任务队列抽取补充（2026-08-30）：普通偏好与 AI 设置共用可测试的串行异步任务队列，单个失败不会阻塞后续本地写入；当前单元测试累计 217 个通过，Chromium/WebKit 偏好与 AI 定向回归各 4/4 通过。

> 最新编辑器写入队列统一补充（2026-08-30）：笔记和来源编辑器共用串行队列，连续保存按顺序执行且不改变原子更新与草稿保护策略；当前单元测试累计 218 个通过，生产笔记/来源回归 Chromium/WebKit 各 17/17 通过。

> 最新 Firefox 环境复验补充（2026-08-30）：重新安装 Playwright Firefox 153.0 后，生产单场景仍在启动阶段无输出并触发 180 秒受控超时；Firefox 结果继续标记为未验证，需稳定版浏览器或 CI 验收。

> 最新 repository 偏好写入边界补充（2026-08-30）：单项与批量偏好写入统一在 repository 门面串行化，覆盖所有本地偏好调用方；lint、类型检查和目标回归通过，Firefox 环境阻塞保持不变。

> 最新完整发布门禁复验（2026-08-30）：最新构建基础质量门禁通过，生产 Chromium/WebKit 各 99/99，移动端生产模拟 6/6；Firefox、真机和内容来源复核仍是发布前置条件。

> 最新移动安全区补充（2026-08-30）：根布局新增 `viewportFit: "cover"`；全局导航、手机吸顶侧栏、离线/Service Worker 状态和 PWA 安装提示均加入 `safe-area-inset` 适配，新增移动布局边界测试；响应式生产回归同时断言生成的 viewport meta 包含 `viewport-fit=cover`。当前单元测试为 26 个文件、220/220 通过；Chromium/WebKit 生产各 99/99，移动生产模拟 6/6。真实 iOS/Android 输入法与安全区仍需真机记录。

> 最新折叠菜单补充（2026-08-30）：首页与子页面的手机导航改为统一可访问折叠菜单，菜单按钮提供展开状态、触控/键盘操作、Escape 关闭并恢复焦点，路由跳转后自动收起；桌面导航不变。新增菜单边界测试和触控回归，当前单元测试为 26 个文件、221/221 通过，移动生产模拟 8/9 通过（iPad Mini 768px 按桌面断点跳过 1 个手机专属菜单用例）；Chromium/WebKit 生产全量各 99/99。

> 最新 PWA 缓存契约补充（2026-08-30）：新增缓存版本一致性测试，校验 Service Worker、生产冒烟和 E2E 断言共享当前 v21，并明确覆盖 v20 旧缓存清理；当前单元测试为 27 个文件、224/224 通过。`npm run verify:release` 的基础门禁、生产 Chromium/WebKit 各 99/99 和移动生产模拟 8/9 均通过。

> 最新开发态 UX 门禁补充（2026-08-30）：无障碍 Chromium 1/1、响应式 Chromium/WebKit 2/2、性能 Chromium/WebKit 4/4 全部通过；该结果与生产全量回归相互独立，真实设备验收仍需人工完成。

> 最新 E2E 稳健性补充（2026-08-30）：核心 Playwright 用例统一提高开发态超时预算，异步页面导航改为等待 DOM 就绪，罗盘修正与复习会话用例等待数据库门禁/成功提示后再操作，并消除重复 `role=status` 的严格定位歧义。相关 WebKit 单场景回归通过；在 2 worker 全量运行中若出现不同用例的 IndexedDB 连接争用或导航超时，应按测试隔离/浏览器资源问题处理并在干净 CI 复验，不视为业务断言通过。

> WebKit 导航边界补充（2026-08-30）：勘误跨标签回归不依赖 `DOMContentLoaded` 事件，改为等待文档 `commit` 和乾卦标题这一明确页面标记；该流程在生产 WebKit 专项 1/1 通过，避免 Service Worker/IndexedDB 启动竞态把可用页面误报为导航失败。

> 生产导航超时补充（2026-08-30）：生产 Playwright 配置为 Chromium/Firefox 导航 30 秒、WebKit 导航 45 秒上限，单测试总预算仍为 120 秒；此前偶发拖满测试预算的勘误与校验和场景在 WebKit 专项 2/2 约 4 秒内通过，页面业务断言和受控重试保持不变。生产 WebKit 99 场景在工作站高负载下默认总预算调整为 300 秒，Chromium/Firefox 仍为 180 秒，均可用 `PRODUCTION_E2E_TIMEOUT_MS` 显式覆盖。

> WebKit 长序列复验说明（2026-08-30）：在本机资源持续抖动时，WebKit 全量运行出现单个用例耗时约 1.6 分钟并触发重试；该次人工中断，不作为全量绿灯证据。页面级专项、基础门禁和此前完整发布门禁结果仍有效，最终发布应以干净 CI/稳定设备的完整结果为准。

> 生产 E2E 重试策略补充（2026-08-30）：生产 Playwright 配置在本地对偶发浏览器启动/IndexedDB 争用允许 1 次受控重试，CI 保持 2 次；确定性断言失败仍会在重试后失败，报告会保留 flaky 标记，不将重试通过误写成首跑全绿。

> 当前配置复验（2026-08-30）：`npm run test:e2e:prod:chromium` 在 2 worker 下完成 99/99；生产运行器同时支持 `--project webkit` 形式的参数解析，CI 工作流已移除统一超时覆盖，确保 WebKit 使用独立预算。

> WebKit 串行复验（2026-08-30）：`npm run test:e2e:prod:webkit -- --workers=1` 在当前构建下完成 99/99（约 2 分钟）；该结果验证完整业务场景，默认 2 worker 的本机资源争用仍按前文限制记录，最终并行门禁以干净 CI 为准。

> WebKit 并行复验（2026-08-30）：当前构建下默认 `npm run test:e2e:prod:webkit`（2 worker）完成 99/99（约 1.2 分钟）；此前本机资源抖动记录保留作历史诊断，稳定版 Firefox 和真实设备仍需外部验收。

> 进度语义补充（2026-08-30）：复习、即时练习、快速自测和首页掌握度进度条均增加 `progressbar` 语义、当前百分比及可读文本；定向生产 Chromium 场景 3/3 通过，WCAG 扫描 1/1 通过。

> 复习队列契约补充（2026-08-30）：到期优先、新卡上限、未知算法隔离和分批续接已抽为 `core/review/queue.ts` 纯函数，新增 4 条单元测试；当前单元测试为 28 个文件、228/228 通过，复习主流程/算法隔离生产 Chromium 场景 3/3 通过。

> 复习队列跨浏览器复验（2026-08-30）：同一组复习主流程、后天方位题和未知算法隔离场景在生产 WebKit 2 worker 下 3/3 通过。

> 复习数据损坏边界补充（2026-08-30）：队列纯函数会再次隔离非法 `dueDate`、非法算法版本并报告数量；首页摘要与复习页共用到期卡判定；正常应用启动前由 `DatabaseGate`/迁移先修复旧记录，因此不会把损坏日期静默当作新卡或到期卡。新增边界回归后当前单元测试为 28 个文件、231/231 通过。

> 最新完整发布门禁（2026-08-30）：基础质量门禁已更新为单测 232/232、lint、类型检查、生产构建 114/114、冒烟 30/30；生产 Chromium 100/100、WebKit 100/100，移动生产模拟 8/9（平板断点专属菜单用例按设计跳过）；Firefox、真机和内容来源复核仍需外部验收。

> 首页到期判定统一复验（2026-08-30）：今日任务与学习概览改用 `core/review/queue.ts` 的共享到期卡判定后，生产 Chromium 全量 E2E 仍为 99/99；该套件覆盖首页即时刷新、孤立记录隔离、复习主流程、备份恢复、PWA、性能与响应式场景。

> WebKit 全量复验（2026-08-30）：共享到期判定改动后，生产 WebKit 默认 2 worker 全量 E2E 完成 99/99；覆盖首页摘要、复习、内容复核、罗盘、备份恢复、PWA、性能与响应式场景。

> UX 专项复验（2026-08-30）：WCAG 2 AA 自动扫描 1/1、Chromium/WebKit 响应式 2/2、Chromium/WebKit 性能 4/4 均通过；当前专项结果覆盖键盘、进度条语义、自测与即时练习答案反馈及保存状态播报、窄屏无横向溢出、索引搜索、爻点击和历法时区规范化。

> 快速自测保存可靠性补充（2026-08-30）：完成结果写入本地失败时，界面保留本轮结果并明确提示“尚未保存”，提供“重试保存”入口；新增组件边界回归，当前单元测试为 28 个文件、232/232 通过，快速自测生产 Chromium 场景 1/1 通过。

> 罗盘记录写入边界补充（2026-08-30）：手工坐向保存前统一归一化角度，再计算八方映射并持久化，确保记录角度始终落在 `[0, 360)`；定向罗盘 Chromium 场景 2/2 和完整基础质量门禁通过。该约束仍只适用于学习型八方记录，不代表传感器测量或二十四山判断。

> 罗盘 repository 守门补充（2026-08-30）：新增领域写入函数，在数据库 repository 最终校验角度归一化结果、八方方向快照、盘层和规则版本；即使未来新增非当前 UI 的写入入口，也不会静默保存不一致坐向记录。当前单元测试为 28 个文件、234/234，生产构建 114/114、冒烟 30/30 通过。

> 罗盘修正 repository 守门补充（2026-08-30）：手动偏差历史在数据库写入边界再次执行 `-180°～+180°` 范围归一化，防止非 UI 调用方写入超限修正；当前单元测试为 28 个文件、236/236，生产构建 114/114、冒烟 30/30 通过。

> 历法时区说明补充（2026-08-30）：历法输入审阅结果现在明确区分带显式偏移的时区转换与无偏移本地时间匹配，避免学习者误以为两种输入都按同一方式处理；规则仍处于待确认状态，不生成农历或干支结果。

> 推演快照 repository 守门补充（2026-08-30）：实验室保存入口在数据库写入边界再次校验上下卦和动爻位置，并重算本卦、变卦派生 ID；未来非当前 UI 的写入也不会保存相互矛盾的推演快照。当前单元测试为 28 个文件、238/238，生产构建 114/114、冒烟 30/30 通过。

> 推演快照重复动爻补充（2026-08-30）：新写入时重复动爻不再静默去重，而是提示数据错误；合法动爻会按初爻到上爻稳定排序，历史迁移仍保持兼容修复。

> 内容勘误写入守门补充（2026-08-30）：勘误保存现在在 repository 层校验状态历史可追溯性、时间顺序、目标和描述字段；不符合契约的未来写入入口会被拒绝，不会产生无法解释的勘误状态。

> 内容勘误历史字段补充（2026-08-30）：状态历史条目采用严格字段边界，未知字段会在普通写入时被拒绝，确保后续状态追踪可解释。

> 收藏写入守门补充（2026-08-30）：收藏保存会在 repository 层确认目标可回链到当前知识点、八卦或六十四卦，并保持稳定主键与有效创建时间，避免产生无法解析的幽灵收藏。

> 收藏浏览器回归补充（2026-08-30）：六十四卦索引跨标签收藏同步、详情收藏与来源流程 Chromium 2/2 通过。

> 课程进度写入守门补充（2026-08-30）：课程开始/完成写入会拒绝非有限分数和未配置练习的未知知识点，避免掌握度出现 `NaN` 或生成无法回链的孤立进度。

> 复习结果写入守门补充（2026-08-30）：复习和即时练习保存前会确认题目快照仍来自当前内容，并校验评价、模式、对错/提示字段和时间边界，避免类型擦除或外部调用写入伪造作答记录。

> 复习结果事务边界补充（2026-08-30）：题目和提交字段校验在打开本地写事务前完成；非法输入不会新增复习卡状态或作答历史。
> 复习备份快照补充（2026-08-30）：当前版本作答记录导入时会核对题目/答案快照；缺少版本号的历史备份仍按兼容路径保留，不会被静默改写。
> AI 审阅状态边界补充（2026-08-30）：本地演示状态机会拒绝未知状态字段、事件字段和缺少文本载荷的事件；真实模型接入仍不在当前发布范围内。
> AI 审阅状态一致性补充（2026-08-30）：待审阅状态会锁定原始草稿文本，必须先进入编辑状态才能产生修改。
> 勘误状态更新补充（2026-08-30）：标记已处理/恢复待处理的原子更新会再次校验完整记录，非法历史或时间不会通过更新入口写入。
> 课程进度时间边界补充（2026-08-30）：课程进度写入现在校验 ISO 时间戳和真实本地日期，异常调用会被拒绝而不会写入掌握度或到期卡。
> 笔记写入边界补充（2026-08-30）：笔记的正文、标签、来源和目标字段现在由 repository 原子新建/更新路径统一校验，非法链接、日期或内容目标不会写入本地数据库。
> 原子主键绑定补充（2026-08-30）：笔记和勘误的删除、撤销、来源更新与状态切换不会接受返回了其他 ID 的记录。
> 笔记时间线补充（2026-08-30）：笔记更新时间和软删除时间现在必须不早于创建时间，异常记录会在写入和备份导入阶段被拒绝。
> 发布级回归补充（2026-08-30）：最新构建在 Chromium/WebKit 生产 E2E 各 100/100 通过，移动生产模拟 8/9（1 个平板断点专属导航用例按设计跳过）；Firefox、真机和内容来源复核仍需外部验收。

> 历法规则版本输入补充（2026-08-30）：历法审阅表单现在显式展示并提交规则版本；用户输入未知版本时显示就地错误，不静默替换为当前草案版本，也不会生成待确认结果。生产 Chromium 专项验证该路径通过。

> Firefox 二进制复验补充（2026-08-30）：本机缓存的 Playwright Firefox 141 与 153 均在首个生产页面启动阶段无测试输出，受控进程最终超时清理；该结果不能替代稳定版 Firefox 或 CI 验收。

> 内容复核映射边界补充（2026-08-30）：内容复核交接包现在除校验数量、ID 和来源外，还严格校验 64 条卦辞的一对一卦象映射，以及每卦初至上六个爻位的 384 条爻辞映射；新增重复卦象、重复爻位和未知卦象回归。当前单元测试为 27 个文件、224/224 通过，生产构建 114/114 通过。

> 计数说明：上方较早的 156、157、159、160、161、162、163、164、165、166、167、168、169、171、172、174、175、176 个测试为历史记录，当前累计以本条 177 个测试为准。

> 计数更新（2026-08-30）：上方 177 个测试为历史快照；加入内容复核映射守护后，当前累计为 27 个测试文件、224/224 个单元测试。

> 版本说明：以下限制条目中的 v59 和早期测试计数属于历史记录；当前 Service Worker 为 v60，最新本地发布门禁已通过 Chromium/WebKit 各 117/117。

1. 当前机器的 Firefox Playwright Nightly 在启动首个页面时持续占用 CPU；受控运行 `FIREFOX_TEST_TIMEOUT_MS=30000 npm run test:e2e:firefox -- --grep "首页实验室"` 或 `PRODUCTION_E2E_TIMEOUT_MS=60000 npm run test:e2e:prod:firefox -- --grep "首页实验室"` 会按超时失败并清理测试与本次启动的生产服务进程，仍需在 CI 或另一台安装稳定 Firefox 的机器复验。
2. 经典底本、64 卦卦辞和 384 条爻辞仍需内容负责人逐条核对来源、版权和版本。
3. 360/768/1024/1440px 自动化无横向溢出检查已通过，但 iOS Safari、Android Chrome、平板真机和输入法遮挡仍需真机记录。
4. 已覆盖键盘核心流程、对比度修复和减少动画 CSS；自动 WCAG 2 AA 扫描已通过，焦点可见性和真实设备仍需人工复验。
5. 已完成真实导出→清空→导入恢复流程，并覆盖 v1→v11 IndexedDB 升级；导入成功后可撤销最近一次导入并恢复导入前数据，快照不进入导出文件；多卡 14 天模拟已通过，长期使用演练仍需执行。
6. Service Worker 只在生产环境注册；当前缓存版本为 `yijing-static-v60`，升级时会清理旧缓存，预缓存采用逐条成功写入，单个路由失败不会阻塞安装，已预缓存品牌图标，只缓存成功的网络响应，运行时缓存写入通过 `FetchEvent.waitUntil` 绑定请求生命周期，导航仅缓存白名单页面并按 pathname 归一化，带查询参数的导航会按 pathname 使用已缓存基础页面，部署后的首屏性能、缓存升级和真实断网网络切换需要再测。
7. 历法规则草案见 `docs/adr/0002-历法与干支时间边界.md`；当前输入审阅会逐项说明换年、换月、换日、子时、二十四节气和真太阳时边界，在规则和样例确认前不提供自动干支排盘。
8. WebKit Playwright 当前在 `context.setOffline(true)` 后进行离线导航会触发浏览器内部错误；生产 PWA 用例在 WebKit 中验证 Service Worker 的 pathname 缓存回退，在 Chromium 中执行真实离线导航，真实 WebKit/iOS Safari 仍需设备验收。
9. 当前工作站的生产 Chromium/WebKit 单浏览器、默认 2 worker 验收均已通过 96/96（WebKit 的 PWA 用例为缓存级 pathname 回退检查）。若需诊断单 worker 的长序列浏览器资源问题，可在命令末尾追加 `--workers=1`；若开发服务器全量运行再次出现页面连接超时，应优先使用生产入口并检查浏览器进程资源，不应直接归因于业务断言。

> 数据迁移补充（2026-08-30）：启动归一化会修复旧笔记、推演快照、勘误和罗盘记录的更新时间顺序；早于创建时间的更新时间会回退到创建时间，早于创建时间的删除时间会被移除，并有自动化回归覆盖。

> 迁移 schema 补充（2026-08-30）：旧笔记来源会按当前声明字段重建并移除未知嵌套字段，避免历史对象在后续编辑时绕过或破坏来源数据契约；新增回归后单元测试为 31 个文件、263/263。

> 最新发布复验（2026-08-30）：迁移 schema 修复后 `npm run verify:release` 全部通过：基础单测 263/263、生产构建 114/114、冒烟 30/30、Chromium 100/100、WebKit 100/100，移动生产模拟 8/9（平板桌面断点菜单用例按设计跳过）。

> 勘误迁移兼容补充（2026-08-30）：旧版已处理勘误会自动补齐可追溯的首个待处理事件，并修复历史时间倒序，后续仍可正常恢复待处理或再次标记已处理。

> 勘误历史追加补充（2026-08-30）：迁移追加的状态事件不会早于已有最后事件，避免旧版时间字段造成新的历史倒序；新增回归后单元测试为 31 个文件、264/264。

> 勘误字段契约补充（2026-08-30）：勘误 repository 写入现在拒绝顶层未知字段，避免新入口静默保存无法导出的数据；回归已纳入内容契约测试。

> 勘误迁移字段补充（2026-08-30）：旧版勘误启动迁移会同步移除顶层未知字段，迁移后仍可正常编辑和导出。

> 笔记全文搜索补充（2026-08-30）：个人知识库搜索已覆盖来源的作者、版本、定位、链接和访问日期，便于从引用元数据回到对应笔记上下文。

> 笔记来源类型搜索补充（2026-08-30）：个人知识库也支持直接搜索中文来源类型名称，与来源下拉筛选保持一致。

> 旧来源搜索兼容补充（2026-08-30）：历史笔记中缺少来源类型的记录会按个人来源参与搜索，与筛选结果保持一致。

> 学习路径导航补充（2026-08-30）：知识详情页“下一步”按 `COURSE_ORDER` 顺序跳转，特殊课程进入河图洛书九宫工具，课程末尾进入复习队列；生产 Chromium/WebKit 回归已覆盖五行课程的下一节链接。

> 干支课程导航补充（2026-08-30）：十天干与十二地支专用页面已加入下一节入口，生产 Chromium/WebKit 的 M2 导航回归各 1/1 通过。

> 最新发布门禁（2026-08-30）：干支课程导航改动后重新执行 `npm run verify:release`，基础单测 266/266、生产构建 114/114、冒烟 30/30、Chromium 101/101、WebKit 101/101、移动生产模拟 8/9（平板专属菜单按设计跳过）；Firefox、真机和内容来源复核仍需外部验收。

> 多来源保护补充（2026-08-30）：来源编辑器编辑第一条引用时会保留其余来源，覆盖多来源笔记的数据可靠性边界；生产 Chromium/WebKit 专项各 1/1 通过，当前基础单元测试为 267/267。

> 多来源新增补充（2026-08-30）：来源编辑器支持追加、任意来源编辑、安全移除及来源元数据/安全外链展示，操作基于最新本地笔记快照执行；v26 缓存升级后的来源/PWA 生产 Chromium/WebKit 专项各 4/4，单元测试 268/268，Firefox、真机和内容来源复核仍需外部验收。

> 知识库来源可见性补充（2026-08-30）：`/notes` 聚合卡片新增来源数量和名称摘要；该页面行为变更已升级 Service Worker 至 v27，并在发布门禁中覆盖缓存版本一致性。

> 跨笔记来源模板补充（2026-08-30）：可在来源编辑器复制当前来源并在另一笔记粘贴，数据只保存在当前浏览器的 localStorage 模板键，不进入 IndexedDB、备份或 AI 请求；模板损坏时会安全禁用粘贴。

> 最新发布门禁（2026-08-30）：来源模板与同页同步修复后的生产 Chromium/WebKit 全量各 102/102 通过，移动生产模拟 8/9 通过（平板专属菜单按设计跳过）；Firefox 稳定版、真机和内容来源复核仍待外部验收。
> 数据清理边界补充（2026-08-30）：设置页清空本地数据前会把来源模板计入确认摘要，并在 IndexedDB 清空成功后移除 `yijing:source-template:v1`；若浏览器拒绝 localStorage 写操作，会明确提示数据库已清空但模板仍需手动处理。
> 导出范围提示补充（2026-08-30）：数据设置页导出预览明确显示来源模板不进入 JSON 备份，并提示其仅保留在当前浏览器、清空本地数据时一并移除；Service Worker 升级至 v29。
> AI 草稿可见性补充（2026-08-30）：AI 本地演示草稿现在直接展示来源引用和“AI 辅助”免责声明，接受/拒绝/编辑前可核对 provenance；不代表真实模型已接入。Service Worker 升级至 v30。
> AI 输入范围可见性补充（2026-08-30）：本地 AI 草稿审阅卡增加授权输入范围中文标签，和外层发送预览保持一致；不改变当前不联网、不保存 AI 输出的限制。Service Worker 升级至 v31。

> 当前发布门禁复验（2026-08-30）：最新 `npm run verify:release` 通过基础门禁（274/274 单元测试、114/114 生产构建、30/30 冒烟）、Chromium 104/104、WebKit 104/104 和移动生产模拟 8/9（平板专属菜单按设计跳过）；Firefox 稳定版/CI、iOS/Android 真机、部署环境和内容来源复核仍待完成。

> 来源追加并发修复补充（2026-08-30）：新增来源的 UI 状态现在直接采用原子事务提交结果，避免跨标签页追加时出现来源计数落后；PWA 缓存版本升级为 v32，上一版本清理回归已纳入生产 E2E。
> 跨标签模板清理回归（2026-08-30）：来源模板移除后的 storage 事件同步已在生产 Chromium/WebKit 各 1/1 验证，其他标签页不会继续保留可点击的旧粘贴入口。
> 内容复核来源策略补强（2026-08-30）：交接包现在拒绝来源收录策略漂移，以及已核验来源缺少该类型必需书目信息或版权字段；新增序列化边界回归。
> E2E 稳定性补充（2026-08-30）：即时练习和来源并发场景改用隔离上下文、真实编辑流程和界面结果读取，移除不必要的裸 IndexedDB 清理连接；定向 Chromium 生产用例已通过 1/1。

> 笔记搜索生产验收（2026-08-30）：来源元数据搜索在生产 Chromium/WebKit 各 1/1 通过；当前基础单元测试为 32 个文件、266/266。

> 备份勘误历史校验补充（2026-08-30）：备份导入现在拒绝勘误记录的状态历史与当前状态不一致、历史时间倒序或顶层时间线倒退，避免恢复后无法继续按追加型生命周期编辑；新增回归测试，当前单元测试为 275/275。

> 导入笔记归一化补充（2026-08-30）：备份导入复用笔记写入边界，修剪/去重标签与来源元数据并拒绝空白来源名称；新增导入归一化回归，当前单元测试为 276/276。

> 导入边界发布复验（2026-08-30）：上述数据契约改动后的完整门禁通过单元测试 276/276、生产构建 114/114、冒烟 30/30、Chromium/WebKit 各 104/104 和移动生产模拟 8/9；Firefox 稳定版/CI、真实设备、部署环境与内容来源复核仍待完成。

> 旧数据枚举迁移补充（2026-08-30）：启动归一化会清洗存在但非法的笔记/复习目标类型和作答可选字段；无法可靠分类的历史作答仅移除非法提示，不伪造目标归属。迁移回归已通过。

> 最新门禁复验（2026-08-30）：上述迁移与 WebKit 静态夹具等待改动后的 `npm run verify:release` 通过单测 277/277、构建 114/114、冒烟 30/30、Chromium/WebKit 104/104 和移动生产模拟 8/9；本次 WebKit 有 1 个首跑超时经受控重试通过并标记 flaky，Firefox、真机、部署与内容来源仍待外部验收。

> 复习预计时间补充（2026-08-31）：复习会话现在使用本地历史作答耗时的稳健中位数估算剩余分钟数；无历史时使用每题 1 分钟默认值，超过 5 分钟的样本不参与估算。该信息仅改善个人学习安排，不改变复习算法、掌握度或隐私边界；新增估算单测及 Chromium/WebKit 生产回归均通过。

> 复习预计时间语义补充（2026-08-31）：当前题保存成功后立即从剩余卡数中扣除，但本次有效作答耗时仍进入后续中位数估算；复习页不会在用户停留于“下一题”按钮时继续把已完成题计入预计时间。生产 Chromium/WebKit 定向场景各 1/1 通过。

> 统计口径补充（2026-08-31）：7 日后记忆率现在明确排除 `reviewMode: "immediate"` 的即时练习，仅使用间隔复习记录；缺少模式字段的旧记录继续兼容。新增回归测试，基础单元测试为 286/286；生产统计页 Chromium/WebKit 各 2/2 通过。

> 统计孤立记录补充（2026-08-31）：到期卡完成率现在要求间隔作答存在对应且受支持的 `ReviewCardState`，孤立作答不会伪造已完成任务；新增回归测试，基础单元测试为 287/287，生产统计页 Chromium/WebKit 各 2/2 通过。

> 今日任务完成摘要（2026-08-31）：首页在当天没有剩余到期卡、但已完成间隔复习时，显示已完成卡片数量和下一次复习日期，并提供继续学习入口；新增 `core/review/today.ts` 汇总契约与首页生产 Chromium/WebKit 各 1/1 回归，基础单元测试为 288/288。

> 最新发布门禁（2026-08-31）：ETA 改动后的 `npm run verify:release` 通过单元 281/281、生产构建 114/114、冒烟 30/30、Chromium 104/104、WebKit 104/104（1 个首跑导航超时经受控重试通过并标记 flaky）和移动生产模拟 8/9；Firefox、真实设备、部署和内容来源仍待外部验收。

> 跨标签通知降级补充（2026-08-31）：`BroadcastChannel` 在标签页挂起或已关闭时发送失败不会再冒泡为写入失败；通知层会关闭失效通道并继续使用 `storage` 事件后备，数据库已提交的本地变更不受影响。新增纯函数边界回归，基础单元测试为 285/285。

> 当前发布门禁复验（2026-08-31）：跨标签通知降级合入后重新执行 `npm run verify:release`，单元测试 285/285、生产构建 114/114、冒烟 30/30、Chromium 104/104、WebKit 104/104（本次无重试）和移动生产模拟 8/9；Firefox 稳定版/CI、真实设备、部署和内容来源仍待外部验收。

> 今日摘要边界补充（2026-08-31）：首页完成摘要现在按当前已知复习卡 ID 同时过滤状态与作答记录，未知卡片不会贡献完成数量或更早的下一次复习日期；`npm run verify:release` 复验通过单元 288/288、生产构建 114/114、冒烟 30/30、Chromium/WebKit 各 105/105、移动生产模拟 8/9。

> 今日日期 rollover 补充（2026-08-31）：今日概览与今日任务增加本地日期变化监听；页面跨过午夜仍保持打开时，仅在日期真正变化后重读学习快照，自动更新待复习数量、连续天数、近七日正确率和完成摘要。新增 `watchLocalDateRollover` 回归，完整发布门禁通过单元 289/289、生产构建 114/114、冒烟 30/30、Chromium/WebKit 各 105/105、移动生产模拟 8/9。

> 统计日期 rollover 补充（2026-08-31）：统计页复用同一本地日期监听，在跨午夜后重新计算 7/30 日窗口、留存、记忆率、到期完成率和薄弱点改善率；不会改变历史记录，只更新当前展示窗口。

> 日期敏感状态补充（2026-08-31）：今日日期标签和知识详情学习状态也复用 rollover 监听，跨午夜后会更新日期文案、待复习状态与下一次复习日期，避免详情页与首页口径分离。

> 日期一致性收口（2026-08-31）：首页、统计页、今日标签和详情学习状态均使用同一 `watchLocalDateRollover` 清理契约；跨午夜更新只触发重新读取/重算，不写入历史数据。完整发布门禁保持单元 289/289、生产构建 114/114、冒烟 30/30、Chromium/WebKit 各 105/105、移动生产模拟 8/9。

> 可见性恢复补充（2026-08-31）：考虑移动浏览器后台冻结/节流 `setInterval` 的行为，日期 watcher 现在在页面恢复可见时立即执行一次检查，并在组件卸载时移除 `visibilitychange` 监听；新增可见性回归测试。最新完整门禁为单元 290/290、生产构建 114/114、冒烟 30/30、Chromium/WebKit 各 105/105、移动生产模拟 8/9；Firefox、真实设备、部署和内容来源仍待外部验收。

> 日期恢复浏览器回归（2026-08-31）：生产 Chromium/WebKit 各新增 1 条 E2E，模拟日期跨午夜并触发可见性恢复事件，确认日期标签即时切换；完整生产门禁更新为 Chromium/WebKit 106/106、单元 290/290、移动生产模拟 8/9。Firefox、真实设备、部署和内容来源仍待外部验收。

> bfcache 恢复补充（2026-08-31）：移动浏览器返回缓存页面时可能只触发 `pageshow`，日期 watcher 已在该事件中立即检查并保持清理契约；Chromium/WebKit 生命周期回归各 1/1 通过，完整门禁维持单元 290/290、Chromium/WebKit 106/106、移动生产模拟 8/9。

> 时间快照一致性补充（2026-08-31）：复习、即时练习和课程进度写入从同一个 `Date` 实例派生 ISO 时间戳与本地日期，避免跨午夜提交产生不一致的日期字段；单元测试 291/291、生产构建 114/114、冒烟 30/30、Chromium/WebKit 106/106、移动生产模拟 8/9 通过。Firefox、真实设备、部署和内容来源仍需外部验收。

> PWA 缓存升级补充（2026-08-31）：时间快照页面行为变更将 Service Worker 从 v32 升级到 v33，并在生产 PWA 回归中验证旧缓存清理；当前缓存版本为 `yijing-static-v33`。

> 数据安全边界补充（2026-08-31）：清空本地数据仅在确有来源模板时展示模板清理结果；导出文件名、信封 `exportedAt` 与 `lastExportAt` 统一使用同一时间快照；新增清空状态纯函数回归，Service Worker 升级至 v34 并验证 v33 旧缓存清理，当前缓存版本为 `yijing-static-v34`。

> 清空文案浏览器回归（2026-08-31）：新增无来源模板的真实清空流程，生产 Chromium/WebKit 各 1/1 通过，确认摘要和完成状态均不误报来源模板；全量生产用例基线更新为 107/107。

> 导出失败语义补充（2026-08-31）：下载已触发但 `lastExportAt` 写入失败时，数据安全页保留“已导出”结果并明确提示元数据未保存；新增导出状态纯函数回归，Service Worker 升级至 v35 并验证 v34 旧缓存清理。

> 外部验收交接补充（2026-08-31）：新增[外部验收与内容交接模板](./05-外部验收与内容交接模板.md)，用于记录内容来源授权、稳定 Firefox、真实 iOS/Android、部署后离线与规则/AI 决策证据；这些证据未补齐前，M0 发布门槛保持未完成。

> 内容复核清单导出补充（2026-08-31）：内容复核清单下载失败时会显示明确的可访问错误状态并清理临时资源；该保护不改变“清单不含经典原文”的数据边界。

> 内容复核导出回归（2026-08-31）：生产 Chromium/WebKit 内容复核专项各 1/1 通过；Blob/下载异常保护已纳入当前构建验证。

> v36 发布门禁复验（2026-08-31）：`npm run verify:release` 通过基础质量门禁、无障碍 1/1、响应式 2/2、性能 4/4、Chromium 108/108、WebKit 108/108（1 条首跑导航超时经受控重试并标记 flaky）及移动生产模拟 8/9；Firefox、真实设备、部署和内容来源仍待外部验收。

> WebKit 运行策略补充（2026-08-31）：生产 WebKit 默认入口改为 1 worker，以减少本机并行浏览器进程导致的导航/IndexedDB 争用；这不改变测试断言，需并行诊断时可显式追加 `--workers=2`。

> WebKit 导航就绪补充（2026-08-31）：生产运行器现在把 `--project` 同步传给配置环境，WebKit 的 45 秒导航预算在命令行入口中确实生效；学习详情 E2E 改为等待 `domcontentloaded` 后再验证交互标题，定向场景 1/1 通过。工作站资源抖动导致的长序列超时仍需在干净 CI 复验，不将外层超时视为业务断言失败。

> 无障碍对比度补充（2026-08-31）：罗盘坐向记录按钮移除 IndexedDB 加载期间的 opacity 过渡，并使用更深强调色，满足 WebKit 对 12px 粗体文本的 WCAG 2 AA 对比度阈值；开发态 Chromium 全路由 axe 扫描与生产 WebKit 定向扫描均 1/1 通过。

> 来源编辑器并发一致性补充（2026-08-31）：保存进入串行队列前固定来源索引，避免跨标签刷新改变选择后把附加来源写回第一条；本地提交时间戳、延迟重读、可见性恢复和低频本地轮询共同防止 WebKit 通知/IndexedDB 可见性竞态回滚已保存来源。最终基础门禁单元 297/297、构建 114/114、冒烟 30/30，生产 Chromium/WebKit 各 108/108；来源并发重复回归 Chromium 5/5、WebKit 10/10。

> 离线状态竞态补充（2026-08-31）：`OfflineStatus` 先注册网络状态监听，再读取 `navigator.onLine` 初始快照，避免水合/导航时离线事件落在监听建立前；断网进入及恢复网络后提示隐藏的生产 Chromium 场景重复 10/10 通过。随后完整 `npm run verify:release` 无失败或重试通过基础门禁、无障碍 1/1、响应式 2/2、性能 4/4、Chromium/WebKit 各 108/108 和移动生产模拟 8/9；Firefox、真实设备、部署和内容来源仍待外部验收。

> Service Worker 缓存升级补充（2026-08-31）：内容复核清单导出行为变更后缓存版本升级至 `yijing-static-v36`，生产 PWA 回归确认旧 v35 缓存清理。

> 下载资源失败保护补充（2026-08-31）：内容复核清单下载使用共享 Blob 下载适配器，URL 创建失败、锚点挂载/点击失败都会在向界面抛出错误前回收已创建资源；成功路径仍延迟 1 秒清理以兼容 Safari/iOS。新增 3 条单元测试，当前质量基线为 36 个文件、296/296。

> 备份下载资源保护补充（2026-08-31）：JSON 备份导出改用同一 Blob 下载适配器，避免浏览器下载 API 在 URL 创建后失败时遗留隐藏锚点或 object URL；备份元数据写入失败的“文件已生成、提醒未保存”语义保持不变。

> 下载清理容错补充（2026-08-31）：URL 撤销或隐藏锚点移除即使被浏览器重复调用而失败，也会被适配器安全吞掉并继续另一项清理；原始下载错误仍交由界面呈现，避免定时器异常污染页面。

> 下载失败浏览器回归（2026-08-31）：生产 Chromium/WebKit 各新增 1 条 E2E，主动让 `URL.createObjectURL` 抛错，确认内容复核清单与 JSON 备份均显示错误且没有遗留隐藏锚点；最新全量为 Chromium/WebKit 各 108/108，WebKit 1 条首跑导航超时经受控重试通过并标记 flaky。

> 统一发布 UX 门禁补充（2026-08-31）：`scripts/verify-release.mjs` 现在在生产浏览器验收前串行执行无障碍、响应式和性能检查；发布命令成功意味着这些自动化门禁也已执行，人工真机与 Firefox 验收边界不变。

> WebKit 首次导航预算补充（2026-08-31）：生产配置将 WebKit 单次导航上限从 30 秒调整为 45 秒，以覆盖 macOS 工作站首次启动 IndexedDB 页面时的偶发冷启动抖动；外层 300 秒套件预算、失败重试和 Firefox 未验收边界保持不变。针对“学习详情可以记录完成状态”的 WebKit 生产专项复验 1/1 通过。

> 设置导航回归（2026-08-31）：设置及其子页面在共享导航中统一显示“设置”为当前入口，并以 `aria-current="page"` 暴露当前状态；生产 Chromium/WebKit 定向回归各 1/1，完整发布门禁各 109/109。

> 中文输入法检索回归（2026-08-31）：六十四卦索引将输入框展示值与已提交筛选值分离，组合输入期间保持当前结果，`compositionend` 后再应用筛选；开发态 Chromium 定向回归 1/1，基础质量门禁单元 299/299、构建 114/114、冒烟 30/30 通过。

> Firefox 启动预检补充（2026-08-31）：生产 Firefox 入口新增独立启动预检，20 秒内无法启动时直接输出 Playwright sandbox/GFX 日志和稳定版 Firefox/可执行文件配置提示；本机当前预检仍因 Playwright Nightly 启动环境失败，未将 Firefox 标记为通过。

> AI 设置重试文案补充（2026-08-31）：AI 设置读取失败后成功重试会清理旧错误文案，恢复为当前授权状态提示；数据库边界回归保持通过。

> 六十四卦搜索无障碍补充（2026-08-31）：筛选结果计数现在通过 `role="status"`、`aria-live="polite"` 和原子更新播报给辅助技术；搜索回归 Chromium 1/1 通过。

> 学习罗盘盘面无障碍补充（2026-08-31）：罗盘可视盘面增加带名称的 `group` 语义，辅助技术可读取当前旋转角度；Chromium 罗盘定向回归 1/1 通过。

> 统计周期键盘补充（2026-08-31）：统计页七日/三十日切换支持方向键、roving `tabIndex` 和焦点跟随，补齐 ARIA tab 控件的键盘操作；Chromium 统计回归 1/1 通过。

> 最终本地门禁复验（2026-08-31）：无障碍扫描对 `/settings` 静态别名的客户端跳转增加最终路由与真实标题文本等待，避免把瞬时元数据误报为 `document-title` 违规。`npm run verify:release` 通过单元 299/299、内容 19/19、构建 114/114、冒烟 30/30、无障碍 1/1、响应式 2/2、性能 4/4、Chromium 110/110、WebKit 109 通过 + 1 条受控重试 flaky、移动 8/9；Firefox、真机、部署和内容来源仍待外部验收。

> ARIA Tab 键盘补充（2026-08-31）：干支关系、河图洛书九宫和学习罗盘的模式 Tab 现在可用方向键切换，并保持单一可聚焦 Tab 与焦点跟随；Chromium/WebKit 定向回归各 3/3 通过。

> ARIA Tab 全量复验（2026-08-31）：三组模式切换合入后 `npm run verify:release` 再次通过；单元 299/299、内容 19/19、构建 114/114、冒烟 30/30、无障碍 1/1、响应式 2/2、性能 4/4、Chromium/WebKit 各 110/110 无失败或重试、移动 8/9。Firefox、真机、部署和内容来源仍待外部验收。

> S6-005 数据可靠性门禁补充（2026-08-31）：新增 `npm run verify:data`，覆盖 14/30 天调度、备份迁移与严格字段、备份提醒、迁移前提醒、repository 事务边界和历法权威样例契约，共 6 个测试文件、139/139 通过；该命令已纳入 `verify:release` 与 CI 静态 job，真实长期浏览器演练仍待外部执行。

> S6-006 Service Worker 缓存边界补充（2026-08-31）：缓存版本升级至 `yijing-static-v37`；导航只缓存白名单页面，并将带查询参数的成功响应归一化写入 pathname，离线读取也始终使用 pathname，避免残留 query 条目污染分享链接页面；生产 Chromium PWA 回归 2/2 通过。

> S6-006 v37 最终发布复验（2026-08-31）：`npm run verify:release` 在缓存版本升级后再次通过；单元 299/299、内容 19/19、数据可靠性 132/132、构建 114/114、冒烟 30/30、无障碍 1/1、响应式 2/2、性能 4/4、Chromium/WebKit 各 110/110、移动 8/9。Firefox、真实设备、部署和内容来源授权仍需外部验收。

> M3-B03 参数边界后的最终发布复验（2026-08-31）：`npm run verify:release` 通过单元 300/300、内容 19/19、数据可靠性 132/132、构建 114/114、冒烟 30/30、无障碍 1/1、响应式 2/2、性能 4/4、Chromium/WebKit 各 110/110、移动 8/9（平板专属菜单按设计跳过）。Firefox、真实设备、部署和内容来源授权仍需外部验收。

> 历法规则证据门禁补充（2026-08-31）：历法规则集 schema 现在要求来源 ID 与权威样例 ID，拒绝重复证据引用；只有 draft 规则集可生成 pending reading，防止未经复核的规则进入正式排盘。完整发布门禁随后通过单元 301/301、内容 19/19、数据可靠性 132/132、构建 114/114、冒烟 30/30、无障碍 1/1、响应式 2/2、性能 4/4、Chromium/WebKit 各 110/110、移动 8/9。

> S6-006 v38 最终发布复验（2026-08-31）：历法审阅页证据状态展示、动态默认时间水合保护和权威样例登记契约合入后，`public/sw.js` 升级至 v38，生产 PWA 回归确认 v37 旧缓存清理；`npm run verify:release` 无失败通过单元 308/308、内容 19/19、数据可靠性 139/139、构建 114/114、冒烟 30/30、无障碍 1/1、响应式 2/2、性能 4/4、Chromium/WebKit 各 111/111、移动 8/9（平板专属菜单按设计跳过）。Firefox、真实设备、部署和内容来源授权仍需外部验收。
> S6-001 生产就绪契约补充（2026-08-31）：无障碍测试等待客户端最终标题，响应式和空来源编辑器回归等待实际交互标记而非晚到的 `load` 事件；完整发布门禁再次通过，Chromium/WebKit 本轮均无失败或重试。

> 历法审阅证据可见性补充（2026-08-31）：历法输入审阅卡现在显示来源 ID、权威样例 ID 的“已登记/待补齐”状态及缺失说明，保持 pending-only 边界，不会因 UI 展示而启用自动排盘；生产 Chromium/WebKit 定向场景各 1/1 通过。

> 历法证据 ID 规范化补充（2026-08-31）：来源与权威样例 ID 拒绝前后空格，保证规则版本和未来备份中的证据引用稳定；未改变自动排盘尚未启用的限制。

## 使用与备份建议

```bash
npm install
npm run dev
```

首次使用建议按“学习地图 → 练习 → 复习 → 笔记”的顺序建立记录。完成一段学习后，在“设置 → 数据安全”导出 JSON 备份；清空或更换浏览器前务必先备份。

## 后续迭代候选

- 完成经典底本、64 卦卦辞/384 条爻辞和来源版权的内容负责人复核；
- 在具备已复核双版本字段后，再开放内容级繁简切换；
- 在稳定版 Firefox、干净 CI、部署环境和真实 iOS/Android 设备上完成发布验收；
- 在历法规则与权威样例确认后，再进入干支计算和节气边界实现；
- 在二十四山来源、流派范围和真实 AI 供应商方案确定后，再推进对应能力。
