# 易境：外部验收与内容交接模板

> 当前验收基线：Service Worker v67 安装/升级（确认 v66 旧缓存清理）。

> 2026-09-05 复习指标一致性补充：今日总结、统计页累计/长期指标、到期完成率、最近学习位置和薄弱点列表现在统一排除结构损坏的复习状态，避免 NaN/非法日期等数据污染学习指标或薄弱点入口；本地 `npm run verify:release` 已复验通过：单元 374/374、内容 34/34、数据可靠性 159/159、构建 114/114、冒烟 30/30、无障碍 4/4、响应式 2/2、性能 4/4，生产 Chromium/WebKit 各 117/117，移动生产模拟 8/9（平板专属菜单按设计跳过）。该结果仍不替代下方 Firefox、真机、部署和内容人工验收。

> 2026-09-05 详情状态一致性补充：详情学习状态现在复用复习状态结构校验，损坏的复习卡显示“数据待修复”；本地完整发布门禁复验通过，基础单元测试 369/369。该变更不替代下方 Firefox、真机、部署和内容人工验收。

> 2026-09-05 复习调度器与队列状态边界补充：`scheduleNext`、复习队列、首页到期判定和“立即再练”现在拒绝越界阶梯、无效本地日期、非有限或负的复习计数、非法薄弱标记及空状态，避免损坏数据被算术传播后写回或展示后才失败；迁移层仍负责修复已知旧记录。最新 `npm run verify:release` 通过：单元 366/366、内容 34/34、数据可靠性 155/155、构建 114/114、冒烟 30/30、无障碍 4/4、响应式 2/2、性能 4/4，生产 Chromium/WebKit 各 117/117，移动生产模拟 8/9（平板专属菜单按设计跳过）。

> 2026-09-05 本地基线补充：日期 rollover watcher 已将轮询间隔限制在 1 秒～24 小时，并增加无效值回退保护；专项与全量单元测试为 363/363，lint、typecheck 通过。该变更不替代下方 Firefox、真机、部署和内容人工验收。

> 2026-09-05 冒烟预算补充：生产冒烟的启动、逐路由和健康探针请求共享 `SMOKE_TIMEOUT_MS` 截止时间；无效值回退 30 秒并通过 `AbortController` 中止超时请求。该变更只改善本地/CI 门禁的失败收敛，不替代部署环境验收。

> 用途：把本地代码已完成、但需要外部资料或设备才能完成的验收项，整理成可直接填写的证据单。  
> 使用方式：每个条目填写“负责人、日期、环境、证据链接/文件、结论”；没有证据时保持 `待验收`，不要用推测替代。

## 0. 当前本地自动化证据

以下结果来自 2026-09-05 最终构建，仅证明本地自动化范围，不替代 Firefox、真实设备、部署或内容负责人验收。

Firefox 预检说明：`scripts/check-firefox-launch.mjs` 默认使用 15 秒启动预算（可用 `FIREFOX_LAUNCH_TIMEOUT_MS` 调整）；`npx playwright install firefox` 当前仅提供 Firefox 153 Nightly，本机预检仍因 macOS sandbox/GFX 初始化超时，需在稳定版浏览器或 CI 环境填写 Firefox 证据。

预算边界说明：开发态与生产态 Firefox 入口会跟随该启动预算设置父进程等待时间（保留诊断余量并限制最大值）；非法测试预算会回退默认值，不应被当作浏览器兼容性证据。

当前页面行为与缓存基线：`/tools/trigrams` 已改为独立八卦工具页面并保留 `/tools` 返回路径；五行工具已补齐 360/768px 移动布局溢出回归；当前 Service Worker 为 v64，部署验收需确认 v63 旧缓存清理。

> 说明：下方早期自动化证据段落中的 v59 为前一轮快照；本轮独立八卦工具路由与 v60 升级以本条及部署检查表为准。

本轮补充：首页“工具”入口统一进入工具集合，五行和干支关系工具详情页返回路径也统一回到工具集合，避免用户从主导航或工具详情离开工具上下文；内容治理页可以下载空白历法样例模板和独立的来源登记表，供内容负责人直接填写版本、链接、版权和复核责任；下载的模板、历法校验报告、内容复核清单和来源登记表都可以再次载入并严格校验；应用会重新计算历法报告的两种覆盖视图，并在通过后逐项展示六类边界的样例 ID、已核验数量和规则集来源；对内容清单执行严格 schema 校验，报告中的篡改数字、未知字段、跨边界样例错配、条目 ID 替换或 accepted 规则未核验来源不会被当作有效证据，并会显示具体错误；来源登记表回载还会校验类型对应的收录策略、已核验来源的必要元数据和复核责任；内容复核清单和来源登记表载入或导出后显示只读摘要、核验计数和发布阻塞项，非法文件会清除摘要且不修改内置内容；单爻笔记目标在详情页、笔记聚合和勘误回链统一使用一到六爻解析契约；来源编辑器追加引用采用字段级幂等原子写入和有界读后写收敛，旧版缺省来源类型按个人记录参与等价去重，避免 WebKit 跨标签旧事务快照覆盖已提交来源；旧复习记录缺失本地日期时按用户本地日历回退；OfflineStatus 已移到数据库门禁外，启动早期即可接收断网事件；五行关系工具新增施受方向说明并修复 360/768px 移动布局溢出，九宫详情、干支关系详情和罗盘测验反馈新增动态状态播报，并完成 Chromium/WebKit 定向回归；详情、今日总结、统计、最近学习位置和薄弱点列表现在隔离损坏复习卡并提供数据设置入口或安全回退。当前 Service Worker 为 v60，单元测试为 374/374；容器全量构建曾因 5 分钟预算超时，调整至 15 分钟后重新构建并通过健康检查与生产冒烟。

生产冒烟默认使用本机临时端口启动 standalone 服务；部署环境验收仍可通过 `SMOKE_BASE_URL` 或 `SMOKE_PORT` 指定固定目标。

容器验收默认使用本机临时宿主端口；如需固定本地映射，可设置 `CONTAINER_PORT`，脚本会将同一端口传给生产冒烟。

| 检查 | 命令/范围 | 结果 |
| --- | --- | --- |
| 基础质量门禁 | `npm run verify:release`（本轮首页工具入口统一进入工具集合、五行和干支关系工具详情返回工具集合、五行工具 360/768px 移动布局修复、复习中心本地混淆点摘要、“立即再练”单题深链、即时练习与间隔复习复述快照、内容治理页历法样例与来源登记表下载/严格回载/只读摘要、内容复核清单严格回载和摘要、单爻笔记目标解析、Service Worker v60、历法规则证据字段、全局静态内容独立校验器、业务 ID 总门禁、备份/交接序列化 ID 校验、AI 输出来源 ID 校验、原始 ID 修剪校验、审阅页展示、水合保护、样例契约、六类边界覆盖审计、内容交接 v2、全局来源索引补强和跨标签来源快照收敛、来源登记表收录策略和必填元数据回载校验、内容复核报告条目 ID 映射校验、生产 E2E 动态端口隔离、生产冒烟动态端口隔离、OfflineStatus 启动阶段断网提示、standalone 启动入口与静态资源同步、容器日志清理守卫、复习指标与薄弱点结构损坏状态隔离） | 基础单测 374/374、内容 34/34、数据可靠性 159/159、构建 114/114、冒烟 30/30、无障碍 4/4（Chromium/WebKit）、响应式 2/2、性能 4/4、Chromium/WebKit 生产各 117/117；移动生产模拟 8/9（1 项按设计跳过）；本轮 WebKit 串行与 Chromium 并行生产全量均 117/117 无失败 |
| 数据可靠性演练 | `npm run verify:data` | 6 个测试文件、159/159；覆盖 14/30 天调度、迁移、复述快照、备份提醒、事务边界、历法权威样例契约与六类边界覆盖 |
| 无障碍 / 响应式 / 性能 | 发布门禁串行步骤 | 4/4、2/2、4/4 |
| 生产浏览器 | Chromium / WebKit 全量 | Chromium 117/117；WebKit 117/117 |
| 移动触控模拟 | Pixel 5、iPhone 13、iPad Mini | 8 通过、1 项平板专属用例按设计跳过 |
| Standalone 容器构建与运行时 | `npm run test:container`（本机 Docker，默认临时端口） | 镜像构建通过；非 root UID 1001；`HEALTHCHECK=healthy`；生产冒烟 30/30；当前环境无 buildx 时自动回退 legacy builder；不替代目标云环境验收 |
| 来源并发回归 | Chromium 5 次、WebKit 20 次 | 全部通过 |
| 断网进入/恢复提示回归 | Chromium 生产重复 10 次 | 全部通过 |
| Service Worker 缓存边界 | v60 白名单与查询参数归一化，Chromium 生产 PWA | 2/2 通过（部署环境仍需确认 v59 旧缓存清理） |

## 1. 内容来源与经典文本交接

### 1.1 来源登记

| 来源 ID | 类型 | 书名/标题 | 作者/频道 | 版本或出版信息 | 定位 | 链接 | 访问日期 | 授权/版权说明 | 复核人 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | classic/book/video/web/personal |  |  |  | 页码或时间点 |  | YYYY-MM-DD |  |  | 待复核 |

填写要求：

- 现代书籍、课程和网页默认只登记必要摘要与定位，不粘贴大段原文；
- `classic` 需明确底本、版本和公版/授权依据；
- `book`、`video`、`web` 必须填写可访问的 HTTP(S) 链接或出版信息；
- 来源状态未变为“已核验”前，不得把卦辞或爻辞标记为已发布。

### 1.2 64 卦与 384 爻复核记录

| 卦序 | 卦名 | 卦辞底本定位 | 初爻～上爻映射 | 内容版本 | 复核人 | 复核日期 | 备注 |
| ---: | --- | --- | --- | ---: | --- | --- | --- |
| 1 |  |  | 逐项填写 6 个爻位 |  |  |  |  |

交接步骤：

1. 从 `/settings/content` 导出不含经典正文的复核清单；
2. 内容负责人在外部底本中逐条核对来源、卦序、爻位和版本；
3. 将复核结果与授权说明交回项目维护者；
4. 更新 `content/hexagrams.ts`、`content/sources.ts` 后运行构建门禁；
5. 只有结构、来源和版权均有证据时，才解除 S6-004 的 `BLOCKED`。

## 2. 浏览器与真实设备验收

### 2.1 Firefox 稳定版或 CI

| 环境 | 浏览器版本 | 操作系统 | 命令 | 结果 | 报告文件 |
| --- | --- | --- | --- | --- | --- |
| GitHub Actions/本机 | Firefox stable |  | `npm run test:e2e:prod:firefox` | 待验收 |  |

验收要求：生产构建通过后，Firefox 全量 E2E 无启动超时；失败时保留 Playwright report 和 `test-results`，不要仅记录“通过/失败”。

### 2.2 真机矩阵

| 设备 | 系统/浏览器 | 授权结果 | 旋转与事件字段 | 稳定性提示 | 手动偏差 | 停止/回退 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| iPhone（型号） | iOS / Safari |  |  |  |  |  |  |
| iPad（型号） | iPadOS / Safari |  |  |  |  |  |  |
| Android（型号） | Android / Chrome |  |  |  |  |  |  |
| Android（型号） | Android / Chrome |  |  |  |  |  |  |

必须确认：

- 用户点击后才请求权限；拒绝或不支持时仍可手动输入角度；
- 页面持续显示“精度未知”、样本状态和磁干扰/姿态提示；
- 停止读取、切换测验、返回历史或离开页面后没有残留事件监听；
- 传感器读数、诊断信息和稳定性样本不写入坐向记录。

## 3. 部署、离线与性能验收

> 当前缓存版本：`yijing-static-v67`；本轮加入数据库内容目录、64 卦程传原注、AI 审计和笔记快捷键，部署验收需确认 v66 旧缓存已清理。

| 项目 | 目标地址/环境 | 结果 | 证据 |
| --- | --- | --- | --- |
| HTTPS 与安全上下文 |  | 待验收 |  |
| `npm run test:smoke`（`SMOKE_BASE_URL`） |  | 待验收 |  |
| 首屏加载与静态资源 |  | 待验收 |  |
| Service Worker v67 安装/升级（确认 v66 旧缓存清理） | 局域网 Docker `http://192.168.3.18:3000` | 待目标手机验收 |  |
| 断网打开已访问页面 |  | 待验收 |  |
| 带查询参数的实验室分享链接离线回退 |  | 待验收 |  |
| 手机网络切换与恢复 |  | 待验收 |  |

部署证据至少保留：目标 URL、构建版本、浏览器版本、Network/Console 截图或日志，以及失败时的复现步骤。

## 4. 规则与 AI 决策输入

### 4.1 历法与干支

代码已提供 `core/calendar/evidence.ts` 样例登记与 accepted 覆盖校验，`/settings/content` 会展示六类边界的逐项覆盖，内容复核 JSON 交接包 v2 也会携带相同覆盖统计。本轮已登记并通过 12 条跨年、跨月、换日、子时、时区和节气样例；外部交接仍需补充更广年份与专业来源，但不再使用空的 pending 占位样例。

- [x] 换年边界：立春瞬间；
- [x] 换月边界：十二节（节气月）；
- [x] 换日边界：当地零点，另支持当地 23 点子初规则；
- [x] 子时拆分方式：按所选换日规则推算时干；
- [x] 时区与夏令时策略：IANA 时区、拒绝重复/不存在本地时间；
- [x] 六类边界各有跨时区/临界样例，状态为 `verified`；
- [x] 规则集登记为 `accepted`，详见 ADR-0009。

### 4.2 二十四山

- [x] 首个流派及适用范围：地盘正针；
- [x] 二十四山名称、角度边界和正五行来源；
- [x] 0°/360° 与左闭右开边界归属；
- [x] 八方/二十四山可切换，盘面点击与键盘高亮共用映射；
- [x] ADR-0009 已登记并通过内容门禁；真机磁偏角与其他流派仍不在本版本范围。

### 4.3 AI 服务

- [x] Responses API 适配器、结构化输出与服务端密钥边界；
- [x] 用户确认文案、范围快照与撤销授权路径；
- [x] `store:false`、无远端会话历史及供应商留存提示；
- [x] 请求失败、超时、离线、配额、体积和进程限流护栏；
- [x] 输出来源引用、免责声明和人工审阅状态机；
- [ ] 配置真实账户密钥后完成在线成功/延迟/费用验收（当前环境无密钥）。

## 5. 发布结论

| 门槛 | 当前状态 | 负责人 | 证据 | 结论日期 |
| --- | --- | --- | --- | --- |
| S6-001 Firefox/跨浏览器 | VERIFY |  |  |  |
| S6-002 人工无障碍 | VERIFY |  |  |  |
| S6-003 iOS/Android 真机 | VERIFY |  |  |  |
| S6-004 内容完整性 | BLOCKED |  |  |  |
| S6-005 长期数据演练 | VERIFY |  |  |  |
| S6-006 部署后离线/性能 | VERIFY |  |  |  |

只有每个 P0 门槛都有可复核证据，且内容授权、真机和部署项不再是 `待验收`，才可以把 M0 发布门槛改为完成。
